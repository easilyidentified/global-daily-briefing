# 제품 방향 — 온톨로지와 상업화

> 세션 로그는 `PROGRESS.md`, 지금 돌아가는 것의 설명은 `CLAUDE.md`.
> 이 파일은 **아직 만들지 않은 것과 그 이유**를 적는다.

---

## 목표 고객

| 구분 | 대상 | 필요한 것 |
| --- | --- | --- |
| **B2C-1** | 기사에 관심 있는 일반 사용자 | 기본 기사 제공. **실시간 업로드**(현재는 하루 1회) |
| **B2C-2** | 기사 고관여자 — 언론고시·면접 준비생, 저널리스트 | 읽고 끝나는 게 아니라 **담고·이해하고·분석**하는 도구 |
| **B2B-1** | 이슈 정보 API 수요자 — 분석 서비스, 저널리즘 | 기사 raw가 아니라 **연결성이 부여된 분석 정보** |
| **B2B-2** | 온톨로지 시스템 수요 기업 | 구축 역량 자체(SI) |

## 예정 기능 (사용자 메모)

- **DB 도입** — 아래 '가장 급한 것' 참조
- **실시간 기사 업로드** — 지구본·이슈 관계도에 실시간 연동되어야 한다
- **개인 기사 스크랩** (B2C-2, 유료)
- **왼쪽 여백: AI 챗봇 창** → 나만의 기사 스크랩 기능(유료)과 연결
- **오른쪽 여백: 온톨로지 대시보드**

---

## 가장 급한 것 — DB, 그리고 공개 리포 문제

지금 구조는 **데이터가 곧 코드**다. `data/issues.json` 한 파일에 전부 들어 있고,
갱신은 봇이 public 리포에 커밋하는 방식이다. 두 가지가 동시에 걸린다.

### 1. 확장이 막힌다

- 기사 한 건 고치려면 JSON 전체를 다시 쓰고 커밋해야 한다
- **실시간 업로드가 원리적으로 불가능하다.** 커밋 주기가 곧 갱신 주기다
- 개인 스크랩·계정·유료화는 쓰기 가능한 저장소 없이는 시작조차 안 된다
- 파일 하나가 계속 커진다. 클라이언트가 매번 전량을 내려받는다
- 온톨로지 계층(L1~L4)은 관계 질의가 본체다. JSON 파일로는 못 한다

### 2. 기술과 데이터가 그대로 공개돼 있다

리포가 public이라 다음이 전부 열려 있다.

| 열려 있는 것 | 왜 문제인가 |
| --- | --- |
| `scripts/collect-prompt.md` | **수집 로직의 실체.** 코드가 아니라 이 지시서가 핵심 자산이다 |
| `data/issues.json` | 축적된 결과물 전체. 경쟁자가 그대로 받아 간다 |
| `.github/workflows/` | 파이프라인 구성 전체 |
| 커밋 히스토리 | 위 전부의 과거 버전 |

**B2B-1(API)과 B2B-2(SI)는 이게 열려 있는 한 팔 물건이 없다.**
지금 파는 것을 그대로 무료 배포하고 있는 셈이다.

### 방향

```
지금        수집 → JSON 커밋 → public repo → 정적 페이지가 fetch
바꾼 뒤     수집 → DB(비공개) → API → 프런트가 질의
            public repo 에는 프런트만
```

- 저장소는 **관계형 + 그래프 질의**가 되는 쪽. 온톨로지가 관계 중심이므로
  Postgres(+ 재귀 질의)로 시작해 부족하면 그래프 DB를 얹는 순서가 무난하다
- 리포를 **private 으로 돌리거나**, 프런트만 남기고 수집·데이터는 분리한다
  (GitHub Pages 를 계속 쓰려면 후자)
- **`collect-prompt.md` 는 최우선 분리 대상**
- 이미 공개된 커밋 히스토리는 리포를 나중에 private 으로 돌려도
  **이미 받아 간 사본까지 되돌리지 못한다.** 판단이 빠를수록 손해가 적다

> 순서상 DB가 온톨로지보다 앞선다. L1~L5 어느 것도 파일 커밋 방식으로는 못 올린다.

## 중요도

`중요도`는 색상 기준 옵션 이름으로 **자리만 잡아뒀다.** 실제 값은 아직 없다.

중요도는 기사 하나만 봐서는 정할 수 없고 그래프에서 파생되는 값이라,
**온톨로지가 붙은 뒤 마지막 단계에서 계산한다.** 계산 근거로 볼 축:

- **지속성** — 같은 스토리라인이 며칠째 살아 있는가
- **파급** — 몇 개국이 다루는가, 엮인 엔티티의 차수
- **갈등도** — 반박 관계, 출처 간 프레이밍 분산
- **출처 다양성** — 몇 개의 독립 출처가 다뤘는가

사람이 라벨을 붙이지 않아도 그래프에서 나온다. 그래서 온톨로지가 선행이다.

---

## 지금 데이터가 가진 것 / 없는 것

가진 것: `topics[]`, `related{type: 유사|반박|다름}`, `why` / `soWhat`,
`context` 4필드, `sources[]`, `date`, `category`, **4개국 동시 수집**.

없는 것 — 온톨로지로 가려면 이 넷이 빈다.

1. **엔티티가 없다.** `topics`는 그냥 문자열이라 이표기가 안 묶인다
   (`이재명` / `이 대통령`, `반도체특별법` / `반도체 특별법`).
2. **사건(Event) 개념이 없다.** 기사가 곧 노드라서, 같은 사건을 4개국이
   각각 보도하면 서로 무관한 노드 4개가 된다. **이 서비스의 최대 자산이
   지금은 데이터에 안 잡힌다.**
3. **시간축 연결이 없다.** 어제 이슈와 오늘 이슈가 이어지는지 모른다.
   `related`는 외부 기사 1건짜리 링크라 사슬을 만들지 못한다.
4. **주장의 귀속이 없다.** `why` / `soWhat`이 누구의 판단인지 없다.
   `반박` 관계가 실질적이려면 필요하다.

## 계층 설계안

```
L0  기사        지금 있는 것. 증거(evidence)로 격하된다
L1  엔티티      인물 / 조직 / 장소 / 정책·법안 / 지표. 정규화 id + 별칭
L2  사건        기사들이 증거로 붙는 상위 노드. 국가 경계를 넘는다
L3  스토리라인   사건의 시간 사슬 (선행 → 후속)
L4  관점        출처·국가별 프레이밍. 같은 사건을 어떻게 다르게 부르는가
L5  중요도      위 네 층에서 계산되는 파생값
```

**차별화의 축은 L2와 L4다.** 국내 경쟁 서비스는 단일 언어권이라
"같은 사건을 한·미·중·일이 어떻게 다르게 다루는가"를 만들 수 없다.
4개국 동시 수집이라는 이미 가진 구조가 여기서 값이 된다.

## 고객별로 온톨로지가 하는 일

- **B2C-1 실시간** — 실시간의 진짜 난점은 수집이 아니라 **중복 제거와 사건 병합**이다.
  L2가 곧 실시간 기능의 전제 조건이다. L2 없이 실시간을 켜면 같은 사건이
  노드 수십 개로 쏟아진다.
- **B2C-2 스크랩** — 스크랩은 곧 '나만의 온톨로지'다. 사용자가 손으로 만든 연결이
  다시 학습 신호로 돌아온다. AI 챗봇은 이 그래프에 근거를 두고 답해야 값이 있다.
- **B2B-1 API** — 팔리는 것은 기사가 아니라 **질의**다.
  "이 사건의 선행 사건", "이 인물이 걸린 이슈", "네 나라가 이 사건을 뭐라 부르는가".
- **B2B-2 SI** — 파이프라인 자체(수집 → 추출 → 연결 → 검증 → 발행)가 레퍼런스가 된다.
  이미 GitHub Actions로 매일 무인 실행되고 있다는 사실이 곧 실적이다.

## 온톨로지로 가는 첫 걸음 (DB 이후)

**`topics`를 문자열에서 엔티티 id로 승격**시키는 것. 별칭 사전을 두고
수집 단계에서 정규화한다. 그것만으로

- `issue-graph.js`의 `similarity()`가 정확해지고 (지금은 표기가 다르면 못 잇는다)
- L2(사건 병합)로 가는 길이 열린다

수집은 이미 AI가 하므로 `scripts/collect-prompt.md`에 추출 지시를 더하는 것으로 끝난다.
한계비용이 거의 없다.

> **주의** — 스키마를 한 번에 크게 바꾸면 매일 도는 파이프라인이 깨진다.
> 새 필드는 전부 optional로 넣고, 화면은 없으면 없는 대로 동작하게 둔다.

---

## 굳어진 설계 결정 (2026-08-12 논의)

나중에 흔들리면 **데이터를 다시 만들어야 하는** 지점들이다.

**1. 국가는 위계의 루트가 아니라 기사의 속성이다.**
`대한민국 > 이슈 > 기사`로 저장하면 호르무즈 사건이 국가마다 복제되고,
그 순간 프레이밍 대조가 원리적으로 불가능해진다. 사건은 국가 무관하게 하나만 두고,
'대한민국 화면'은 `기사.country = KR` 필터로 만든다.

**2. 사람이 정하는 것과 데이터에서 나오는 것을 나눈다.**

| | 누가 | 내용 | 규모 |
| --- | --- | --- | --- |
| 스키마 (T-Box) | 사람이 한 번 | 엔티티 타입·관계 타입 | 10개 남짓, 고정 |
| 인스턴스 (A-Box) | 데이터에서 | 개별 사건, 이슈라인 | 무한 |

개별 사건과 이슈라인을 미리 정의하려 들면 실패한다. 새 사건이 기존 분류에 안 들어가고,
추상화를 올리면 결국 철학("국가간 안보 경쟁")이 된다. **최상위 층은 만들지 않는다.**

**3. 묶음(이슈라인)은 노드가 아니라 경로다.**
"호르무즈 이슈라인을 만들자"고 선언하지 않는다. 사건끼리 잇다 보면 연결된 덩어리가
생기고 그것이 곧 이슈라인이다. 이름은 3건쯤 이어져 안정된 뒤에 붙인다.

연결 조건 — 셋 다 판정 가능하고 반박 가능해야 한다.
① 엔티티 공유 ② 시간 순 ③ 후속·인과가 **기사에 서술돼 있음(근거 문장을 남긴다)**

> 실제로 연결 근거는 이미 본문에 있다. `flashes.json`의
> "7월 7일 알레카야트에 이어 한 달 새 두 번째입니다" 한 문장이
> 두 사건을 잇는 증거인데 지금은 요약 안에 묻혀 추출되지 않을 뿐이다.

**4. 사건은 백엔드 단위, 이슈라인은 화면 단위.**

```
기사      24건/일        누적 증가
사건      15~20건/일      누적 증가   ← 많아도 된다. 인덱스다
이슈라인   국가당 활성 5~15개          ← 화면에 나가는 것. 수명이 있어 안 늘어난다
```

`한국 > 핫이슈 > 사건`은 **질의 결과**지 저장 구조가 아니다.
핫이슈는 매일 바뀌고 국가마다 다르므로 위계에 넣으면 매일 트리를 다시 만들어야 한다.

**5. 추출 원문을 버리지 않는다.**
정규화된 그래프와 별개로 AI가 뽑아낸 날것을 보관한다. 그래야 관계 타입을 바꿔도
재수집 없이 다시 계산할 수 있다. 안 해두면 스키마를 바꿀 때마다 4개국 과거를 다시 읽어야 한다.
타입 **추가**는 싸고 **통합·분할**은 비싸다 → 처음에 이름을 보수적으로, 적게.

## 연관 이슈 전개 (핫이슈 클릭 → 그래프)

연관을 한 종류로 두면 실뭉치가 된다. 네 종류로 나눈다.

| 종류 | 예시 | 난이도 |
| --- | --- | --- |
| 엔티티 공유 | 민주당 전당대회 ↔ 2026 세제개편 | 쉬움 |
| 인과 | 호르무즈 피격 → 유럽 가스값 → 한국 LNG 단가 | 중간 |
| **선례 (과거)** | 2026 세제개편 ↔ 2020 세제개편 | **어렵고 제일 값짐** |
| 대립 | 같은 사건, 충돌하는 관점 | L4 있으면 나옴 |

- **선례에는 시간 깊이가 필요하다.** 현재 국가당 30건(약 5일치)로는 값이 안 나온다.
  → **과거 기사 소급 수집**이 선행 조건. 수집 파이프라인을 날짜만 바꿔 돌리는 일회성 작업
- **흔한 엔티티로는 잇지 않는다.** `정부`·`국회`·`미국`은 거의 모든 이슈에 나온다.
  흔함도(IDF) 가중이 필요하다. *지금 `similarity()`의 `topics` 자카드가 이미 가진 결함이다 —
  `반도체`와 `국회`를 똑같이 취급한다*
- **한 번에 다 펼치지 않는다.** 사건 사슬 → 연관 라인 몇 개 → 누르면 더. 누른 만큼만 연다

## 선행 사례 (2026-08 조사)

| 시스템 | 목적 | 구조 | 우리와의 관계 |
| --- | --- | --- | --- |
| **GDELT** | 거시 지표·계량 연구 | CAMEO 이벤트 코딩(actor1/actor2/event/tone), 2억 건+ | 규모는 압도적이나 문장 단위 얕은 추출. 엔티티 연결 약함 |
| **Event Registry** | 사건 단위 뉴스 검색 | 기사 클러스터링 → event, 다국어 동일 사건 병합 | **상업 서비스 중 가장 가까움.** 병합까지는 하되 관점 비교는 없음 |
| **EventKG** | 사건 중심 시간 지식그래프 | Wikidata·DBpedia 통합. 69만 사건, 230만 시간관계 | 백과사전형(과거 사건). 실시간 뉴스가 아님 |
| **NewsReader + GRaSP** | 4개 언어 뉴스 → 사건 중심 RDF **+ 관점** | instance / mention / attribution 3층 분리 | **우리 구상과 가장 가까움.** 다만 연구 프로젝트고 제품이 아니다 |
| **ClaimsKG** | 팩트체크된 주장 질의 | ClaimReview 스키마, 2.8만 주장 / 600만 트리플 | 주장 귀속의 기성 스키마. 참고 대상 |
| **IPTC NewsML-G2 / Media Topics** | 뉴스 업계 교환·분류 표준 | SKOS 기반 주제 체계 | 카테고리를 표준에 맞추면 B2B 납품이 쉬워진다 |
| **Palantir Foundry Ontology** | 조직의 디지털 트윈 | **objects + links + actions** | 4번 항목 참조 — actions가 결정적 차이 |

인프라 계층 참고: `news-please`(크롤러), Media Cloud(미디어 계량),
`alephdata/aleph`·OpenAleph(문서·엔티티 검색, 탐사보도용),
Diffbot Knowledge Graph(상업 KG), `getzep/graphiti`(에이전트용 실시간 KG).

**핵심 관찰** — 대부분이 *추출*까지 가고 *관점·귀속*까지 가지 않는다.
거기까지 간 NewsReader/GRaSP는 상품이 아니다. **빈 자리가 여기 있다.**

## 최종 화면 — 온톨로지가 실제로 보이는 곳

온톨로지는 그 자체로 제품이 아니다. 아래 넷이 사용자가 만나는 얼굴이다.

1. **사건 카드** — 지금 이슈 카드의 진화형. 정본 + 증거 출처 목록
2. **스토리라인 타임라인** — 선행 → 후속. 지금 관계도가 못 하는 것
3. **프레이밍 대조 뷰** — 같은 사건, 4개국 4열. **이 서비스의 시그니처**
4. **엔티티 프로파일** — 인물·조직·정책별 이슈 이력

사용자 메모의 두 창이 여기 맞물린다.
왼쪽 AI 챗봇 = 그래프에 근거를 둔 질의 인터페이스(노드·엣지를 인용해 답한다).
오른쪽 온톨로지 대시보드 = 위 네 뷰의 진입점.

**팔란티어의 actions 자리에 무엇을 놓을 것인가.**
우리는 세계를 바꾸는 실행(Action)을 할 수 없다. 읽기 전용 지식그래프는 결국 조회 도구에 그친다.
유일하게 가능한 쓰기는 **사용자가 그래프에 남기는 것** — 스크랩, 내 견해, 내가 만든 연결.
B2C-2 유료 기능이 곧 actions 자리를 차지한다. 그게 있어야 제품이 된다.
