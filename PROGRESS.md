# 진척 기록

세션 단위로 **위에서부터 최신순**으로 쌓는다. 각 항목은 `한 일 / 확인한 사실 / 다음 할 일` 구조.
프로젝트 구조와 규칙은 `CLAUDE.md`에 있다. 여기는 **시간순 기록**만 남긴다.

---

## 2026-08-11 (17) — 속보 3건 교체(12개국 제약 해제), 메일 숨은참조 처리

### 1. 속보를 12개국 밖에도 꽂을 수 있게 (`globe.js`)

`addFlash`가 `COORDS[f.code]`만 봤다. 목록 밖 국가는 `if (!c) return`으로 **조용히 사라졌다.**
`f.latlng`를 먼저 보도록 바꿔 좌표를 직접 주면 어디든 꽂히게 했다.
`focusOn(code, latlng)`도 같이 확장해 칩 클릭 시 회전이 그 지점을 향한다.

### 2. 속보 3건 교체 — 우주선은 손대지 않음

`starship-moon`(`kind: space`, `ship: true`)은 **한 글자도 건드리지 않았다.**
과거 속보 `kr-sidecar`(코스닥 사이드카)를 빼고 세 건을 넣었다.
전부 실제 검색 결과의 URL이며 **4건 모두 HTTP 200 확인**했다.

| id | 국가 | 날짜 | 내용 |
| --- | --- | --- | --- |
| `co-quake-choco` | CO (4.89, -76.23) | 08.10 | 규모 7.4 지진, 최소 20명 사망. 400km 밖 보고타까지 대피 |
| `ru-nizhnekamsk-drone` | RU (55.64, 51.82) | 08.10 | 드론이 모스크바 동쪽 800km 정유공장 타격, 13명 사망 |
| `hormuz-lng-tanker` | OM (26.57, 56.25) | 08.03 | 한 달 새 LNG 유조선 두 척 피격, 카타르에너지 force majeure |

**세 국가 모두 `COORDS` 밖이다** — 1번 수정이 없으면 하나도 안 뜬다.
색은 현재 테마의 강조색 `#ec3013`으로 통일했고, 우주선만 기존 `#1f6feb`를 유지한다.

### 3. 메일 수신자 숨은참조 (`send_briefing.py`)

`BRIEFING_TO`가 26명으로 늘었는데 `msg["To"]`에 **전원을 그대로 적고 있었다.**
받는 사람 26명 모두가 서로의 주소를 보게 된다.

- SMTP는 **봉투(실제 배달 목록)** 와 **To 헤더(적힌 수신자)** 가 별개다.
  봉투에는 전원, 헤더에는 한 명만 넣으면 나머지는 그대로 숨은 참조가 된다
- `BRIEFING_TO_VISIBLE`(선택) 신설. 비워 두면 `BRIEFING_TO`의 **맨 앞 주소** 하나만 보인다.
  현재 값이 `kimgt0530@gmail.com`으로 시작하므로 **설정 변경 없이 의도대로 동작한다**
- `Bcc` 헤더는 넣지 않는다 — 넣으면 숨기려던 주소가 그대로 노출된다
- **로그에서 주소를 뺐다.** 공개 리포의 Actions 로그는 누구나 볼 수 있는데
  기존 코드는 수신자 전원을 평문으로 찍고 있었다. 이제 숫자만 남긴다

### 검증

메일은 **한 통도 보내지 않았다** (아침에 나가야 하므로). `smtplib.SMTP_SSL`을 가짜로 갈아끼워
네트워크 없이 실제로 만들어지는 봉투와 헤더만 들여다봤다.

| 항목 | 결과 |
| --- | --- |
| 실제 배달 인원 | 26명 ✅ |
| `To` 헤더 | `kimgt0530@gmail.com` **한 명** ✅ |
| `Bcc` 헤더 | 없음 ✅ |
| 나머지 25명 주소가 헤더·본문에 샜는가 | **0건** ✅ |

속보는 헤드리스 Chrome으로 확인 — 4건 전부 칩 렌더, 지구 표면 반지름 1.03에 정확히 안착,
칩 클릭 시 카드 제목·출처 링크 정상, JS 오류 0.

### 4. 실제 발송 테스트 (사용자 요청) — 그 과정에서 버그 2개

수동 실행은 신선도 가드(`latestDate` 08.10 ≠ KST 08.11)에 막혀 발송까지 못 간다.
테스트용 우회 스위치를 넣는 과정에서 두 가지가 걸렸다.

**버그 A — GitHub 표현식으로 값을 비울 수 없다**

```yaml
EXPECT_DATE: ${{ inputs.force && '' || steps.kst.outputs.date }}   # 동작하지 않는다
```

GitHub 표현식에서 **빈 문자열은 falsy**다. `A && '' || B`는 언제나 `B`로 떨어진다.
`force=true`로 돌려도 EXPECT_DATE가 그대로 들어가 skip 됐다.
`EXPECT_DATE`는 항상 넘기고 **우회 여부는 `FORCE_SEND`로 따로 넘겨 파이썬에서 판단**하도록 바꿨다.

**버그 B — `BRIEFING_TO_VISIBLE`이 스크립트까지 오지 않았다**

변수는 등록돼 있는데 `send-briefing.yml`의 `env` 블록에 없었다.
`visible`이 `BRIEFING_TO`의 맨 앞 주소로 조용히 폴백돼 **설정한 표시 주소가 무시**되고 있었다.
로그의 `숨은참조 0명`이 단서였다 — 숫자를 안 찍었으면 못 잡았다.

**결과** — run `31403982200`

```
BRIEFING_TO_VISIBLE: kimgt412@korea.ac.kr
[force] ... FORCE_SEND로 검사를 건너뜁니다.
[send] 발송 완료 → 배달 1명 · To 헤더 1개 · 숨은참조 1명
```

스케줄 실행에는 `inputs`가 없어 `FORCE_SEND`가 빈 값이므로 **오발송 방지는 그대로 살아 있다.**
로컬에서 두 경로를 모두 확인했다 (없으면 skip, `true`면 우회).

> **아침 전에 되돌릴 것**: 테스트하느라 `BRIEFING_TO`가 `kimgt0530@gmail.com` 한 명으로 줄어 있다.
> 26명 목록을 복구하지 않으면 내일 아침 한 명에게만 나간다.

---

## 2026-08-11 (16) — **수집이 UTC 날짜를 쓰던 치명적 결함 수정** + `.gitignore` + 종료 점검 규칙

"내일 아침 메일이 제대로 갈까"를 점검하다 **첫 자동 실행이 통째로 빌 결함**을 찾았다.
크론이 뜨기 6시간 40분 전에 잡았다.

### 결함 — 에이전트가 KST 날짜를 알 방법이 없었다

```
수집 크론 21:40 UTC  →  UTC 날짜 08.10 / KST 날짜 08.11   ← 하루가 어긋난다
```

- `collect-prompt.md`는 `date`를 "KST 기준 오늘 날짜"라고만 적어 뒀지 **구하는 방법이 없었다.**
- 에이전트의 `--allowedTools`는 `Read,Write,Edit,WebSearch,WebFetch` — **`Bash`가 없다.**
  `TZ=Asia/Seoul date`를 실행할 수단 자체가 없다.
- 워크플로도 `TZ`를 주입하지 않았다. 러너는 UTC다.

**연쇄 결과**

```
에이전트가 오늘을 2026.08.10으로 판단
  → 0단계 "latestDate와 같거나 이전이면 건너뛴다" → 4개국 전부 skip
  → data/issues.json 무변경 → "변경 없음 — 커밋하지 않습니다" → 푸시 없음
  → 08:00 KST 발송이 EXPECT_DATE(2026.08.11) ≠ latestDate(2026.08.10) 확인 → skip
  → 메일 안 감
```

### 왜 여태 안 드러났나

성공한 실행이 전부 **UTC 오전~오후 수동 실행**이었다 (`31363178855`는 06:45 UTC).
그 시간대는 UTC 날짜와 KST 날짜가 같아서 결함이 드러나지 않는다.
**21:40 UTC 스케줄 실행이 이 경로를 처음 밟는다.**

### 수정

- `daily-briefing.yml`에 **`Compute today's KST date` 스텝 신설** →
  `${{ steps.kst.outputs.date }}`를 **프롬프트 첫 줄에 박아서** 넘긴다.
  `send-briefing.yml`이 이미 쓰던 것과 **같은 패턴**이라 동작이 검증된 방식이다
  (그 워크플로 로그에 `EXPECT_DATE: 2026.08.10`이 KST 기준으로 정확히 찍힌다)
- 같은 스텝에 `env: TZ: Asia/Seoul` 도 걸어 이중으로 막았다
- `collect-prompt.md` 0단계 맨 앞에 **"오늘 날짜는 스스로 정하지 마라"** 절 신설 —
  왜 어긋나는지와 어긋났을 때 무슨 일이 나는지까지 적었다
- `CLAUDE.md` 주의 항목도 같은 내용으로 교체

### 확인한 것 — 발송 경로 자체는 멀쩡하다

수동 실행 `31398296071` 로그에서 **`[send] 발송 완료 → 2명`** 을 직접 확인했다.
건너뛰고 success 난 게 아니라 실제로 보냈다. 렌더도 정상(HTML 22KB, 배너 `cid:` 인라인).

> 로컬 dry-run은 435KB로 나오는데 이는 배너를 data URI로 박기 때문이고, 실제 발송은 `cid:`라 22KB다.

### `.gitignore` 신설

`.claude/`, `claude-design-변경사항/`, `preview_*.html`, `__pycache__/`, OS 잡파일.
`git check-ignore`로 세 항목 모두 실제로 걸리는 것을 확인했다.

### 세션 종료 점검 규칙 (사용자 지시)

`CLAUDE.md` 세션 운영 규칙 3항을 **"맥락 파일 커밋"에서 "리포지토리 전반 점검"으로 확대**했다.
커밋 상태 / 맥락 3종 / 코드 동작 / 자동화 상태 / 새 파일 취급 — 5개 항목 체크리스트.

### 남은 위험과 관측 문제

1. 에이전트가 주입된 날짜를 **실제로 따르는지**는 내일 실행으로만 확인된다
2. **발송이 건너뛰어도 워크플로는 success로 끝난다.** `success == 발송됨`이 아니다.
   판단하려면 `Render and send briefing` 스텝 로그에서 `[send] 발송 완료`를 봐야 한다

---

## 2026-08-10 (15) — 맥락 파일 3종 최신화, 진척 로그 정렬 복구

"진척사항도 최신화·동기화 되었나"를 점검하다 **세 가지가 밀려 있는 것**을 찾았다.
`PROGRESS.md`는 원격과 일치했지만 나머지 둘은 오늘 작업을 전혀 담고 있지 않았다.

### 1. `PROGRESS.md` 정렬이 깨져 있었다

이 파일의 규칙은 "위에서부터 최신순"인데 실제 순서가 이랬다.

```
9, 14, 13, 12, 11, 10, 8, 7, ... 1      ← (9)만 맨 위에 남아 있었다
```

세션 (9)를 맨 위에 넣은 뒤, (10) 이후를 전부 **(9) 아래에** 끼워 넣은 탓이다.
세션 번호 내림차순으로 다시 정렬했다 (`14 … 1`). 순수 이동이라 내용 변화는 없다
(61 insertions / 61 deletions, 본문 손실 0).

세션 (9)의 "아래 항목 참조"는 정렬 후 방향이 뒤집히므로 "위 세션 (10) 참조"로 고쳤다.

### 2. `CLAUDE.md`가 이메일 파이프라인을 몰랐다

**세션 시작 시 자동으로 읽히는 파일**인데 `send_briefing.py` / `send-briefing.yml` /
`assets/`가 파일 구조 표에 없었고, 자동화 절에는 수집 크론 하나만 적혀 있었다.

- 파일 구조 표에 3개 항목 추가
- 자동화 절을 **수집 / 발송 2단 구조**로 재작성 (06:40 → 08:00 순서를 명시)
- 발송 쪽에 시크릿·변수 목록, 오발송 방지 규칙, `DRY_RUN=1` 미리보기 방법,
  `preview_*.html`을 커밋하지 말 것, `why`/`soWhat`이 최상위 필드라는 점을 적었다

### 3. `github.md` 스냅샷이 `905697d`(cron 활성화)에 멈춰 있었다

세션 (9)~(14)의 관계도 요약 카드 작업과 이메일 파이프라인이 통째로 빠져 있었다.
`Updated in this project`를 파이프라인 / 관계도 / 모바일 세 덩어리로 다시 썼고,
Screen map에 브리핑 이메일 행을 추가했다.

### 알아둘 것

`.gitignore`가 없는 리포다. 작업 폴더에 `.claude/`와 `claude-design-변경사항/`이
untracked로 남아 있으니 `git add .`을 하면 함께 들어간다.
자동 수집 워크플로는 `data/issues.json`만 스테이징하므로 자동 커밋에는 영향이 없다.

---

## 2026-08-10 (14) — 원격 변경 동기화, 이메일 발송 파이프라인 점검

사용자가 따로 올린 커밋 7개를 받아 fast-forward 했다. 이쪽에 앞선 커밋은 없어 충돌 없음
(`cb0562a → 01f82b1`, 0 ahead / 7 behind).

### 들어온 것

| 파일 | 내용 |
| --- | --- |
| `scripts/send_briefing.py` | Gmail SMTP 발송 스크립트 (299줄, 표준 라이브러리만) |
| `.github/workflows/send-briefing.yml` | 매일 08:00 KST 발송 (`cron "0 23 * * *"`) |
| `assets/email_banner.png` | 메일 상단 배너 (310KB, `cid:` 인라인 첨부) |
| `issue-graph.js` | 토글 라벨 `관계 요약`/`그래프만` → `on`/`off`, 안내 문구 → `기본 창 토글` |

### 점검 결과 — 발송 파이프라인은 이미 동작 중

- **dry-run 실측**: `DRY_RUN=1`로 KR 렌더 성공. HTML 436KB,
  Why/So What 6쌍, 정치·경제·기타 3개 섹션, 출처 6건, 함께읽기 4건, 미치환 플레이스홀더 0
  - 함께읽기가 4건인 건 세션 (3)에서 확인한 **KR `related` 누락 2건**과 정확히 맞는다
- **시크릿·변수 등록 완료**: `GMAIL_USER`, `GMAIL_APP_PASSWORD` (secret) /
  `BRIEFING_TO`(2명), `BRIEFING_FROM_NAME` (variable)
- **수동 실행 2회 모두 success** (`31396022061`, `31398296071`)
- **오발송 방지 장치 확인**: `EXPECT_DATE`(KST 오늘)와 `latestDate`가 다르면 그냥 건너뛴다.
  수집이 실패한 날 어제치를 다시 보내는 사고가 구조적으로 막혀 있다

`why`/`soWhat`가 `context` 안이 아니라 **이슈 최상위**에 있는 게 맞는지 스키마로 확인했다 — 맞다.

### 두 크론의 관계

```
06:40 KST  daily-briefing.yml   수집 (실측 13~24분)
08:00 KST  send-briefing.yml    발송 — 위가 끝난 뒤 여유 있게 돈다
```

내일(08-11) 아침 처음으로 둘이 연달아 돈다.

### 기록 정정

세션 (9)·(10)의 `overflow:hidden` 서술을 고쳤다. 해당 항목 참조.

---

## 2026-08-10 (13) — 점프 착지점을 '오늘의 주요 이슈' 표로, 버튼 라벨 교체

### 1. 안내 문구 재수정

```
창/노드를 클릭하면 연관 이슈를 볼 수 있습니다.
(작은 화면 비율에서는 보이지 않을 수 있으니, 화면을 돌려주세요)
본 사이트는 데스크탑에서 가장 잘 작동합니다.
```

세 번째 줄 앞의 `*`를 뺐다. 구조는 세션 (12)의 `.graph-note` 그대로다.

### 2. '더 자세히 보기'가 표로 가게

`jump()`가 `#issue-<key>`(= '분야별 주요 소식 및 맥락 분석' 스트림 항목)로만 가고 있었다.
`#row-<key>`를 먼저 찾도록 바꿨다.

```js
const el = document.getElementById('row-' + item.key) || document.getElementById('issue-' + item.key);
```

착지할 자리가 없어서 표 쪽에 먼저 만들어줬다.

- `rows`에 `domId: 'row-' + n.id` 와 `rowBg`(포커스 시 `#ffe9e5`) 신설 —
  스트림 항목이 쓰던 `domId`/`rowBg` 패턴을 그대로 따랐다
- `<tr>`에 `id="{{ r.domId }}"` + `background:{{ r.rowBg }}` + `scroll-margin-top:24px`

**폴백이 필요한 이유**: 표(`rows`)는 `today`(= `date === anchor`)만 담는다.
관계도에는 과거 노드도 있고 그걸 핀하면 표에 대응 행이 없다.
그래서 `||`로 스트림 항목에 떨어뜨린다. `cat`은 그대로 세팅하므로 폴백 시 탭도 맞는다.

### 3. 버튼 라벨

`issue-graph.js`의 full 카드 버튼 `'요약으로 이동'` → **`'더 자세히 보기'`**.

### 검증 — CDP로 버튼을 실제 클릭

| 대상 | 버튼 라벨 | 표 행 | 착지 |
| --- | --- | --- | --- |
| **오늘 이슈** `kr-pol-20260810-1` | 더 자세히 보기 | 있음 | **표 행이 뷰포트 top 24px** (스트림 항목은 667px 아래) |
| **과거 이슈** `kr-pol-1` (08.09) | 더 자세히 보기 | 없음 | **스트림 항목이 top 24px** (폴백 정상) |

- 표 행 6개에 `row-kr-pol-20260810-1` … 형태로 id가 붙었다
- 점프 직후 대상 행만 `rgb(255,233,229)`로 강조된다 (`highlightedRows` 1건)
- 안내 문구: 4개 폭(1440/768/390/360) 전부 3줄 렌더, 행 밖 이탈·가로 넘침 없음,
  미해결 템플릿 변수 0, JS 오류 0

---

## 2026-08-10 (12) — 관계도 안내 문구 교체 (3줄)

`Globe Briefing.dc.html:204`의 `점을 클릭하면 해당 이슈 요약으로 이동`을 3줄로 바꿨다.
세션 (9)~(11)에서 상호작용이 '점 클릭 → 요약 이동'에서 '창 클릭 → 연관 이슈'로 바뀌었으니
문구도 실제 동작과 어긋나 있었다.

```
창을 클릭하면 연관된 과거의 이슈들이 보입니다.
(작은 화면 비율에서는 보이지 않을 수 있으니, 화면을 돌려주세요)
* 본 사이트는 데스크탑에서 가장 잘 작동합니다.
```

- `<br>`은 이 템플릿에서 한 번도 안 쓰였다(0건). 굳이 처음 쓰지 않고
  `display:block` span 3개를 `.graph-note` div로 묶었다
- 데스크탑은 `margin-left:auto` + `text-align:right`로 기존 위치 유지
- **모바일은 좌측 정렬로 뒤집는다** — 줄마다 접히는데 오른쪽 정렬이면 왼쪽이 들쭉날쭉해 읽기 나쁘다.
  `@media (max-width:768px)`에 `.graph-note` 규칙 신설, 자간도 `.12em → .06em`

### 검증

| 폭 | 3줄 | 정렬 | 행 밖 이탈 | 가로 넘침 | 미해결 템플릿 변수 |
| --- | --- | --- | --- | --- | --- |
| 1440px | ✅ | right | 없음 | 없음 | 0 |
| 768px | ✅ | left | 없음 | 없음 | 0 |
| 390px | ✅ | left | 없음 | 없음 | 0 |
| 360px | ✅ | left | 없음 | 없음 | 0 |

옛 문구는 DOM에서 사라졌고(`oldTextLeft: false`) JS 오류 0건.

---

## 2026-08-10 (11) — 요약 카드도 노드처럼 클릭·호버되게

노드를 클릭하면 나머지가 흐려지고 연결된 노드가 강조되는데,
**상시 떠 있는 요약 카드는 클릭이 안 먹었다.** 카드가 노드의 대역이 되도록 맞췄다.

### 원인

`buildCard`에서 mini 카드에 `pointer-events:none`이 박혀 있었다.
full(핀된) 카드만 `auto`라서 상시 카드는 마우스를 아예 받지 못했다.

### 적용

- mini 카드도 `pointer-events:auto` + `cursor:pointer`
- **클릭** → `pin(this.pinned === n ? null : n)` — 노드 클릭과 같은 진입점
- **호버** → `this.hover = n` + 테두리 강조색. 노드에 마우스를 올린 것과 동일하게
  나머지가 흐려지고 해당 클러스터만 남는다
- 핸들러는 mini에만 단다. full 카드는 이미 '요약으로 이동'·'닫기' 버튼이 있어
  박스 전체에 클릭을 걸면 버튼과 부딪힌다
- **`pin()`에서 `this.hover = null`** — 커서 밑의 카드가 곧 재생성되므로
  그 카드의 `pointerleave`가 영영 안 온다. 안 지우면 핀을 풀어도 흐림이 남는다

### 검증 — CDP로 실제 마우스 이벤트를 쏴서 히트 테스트까지

| 단계 | pinned | 카드 | 강조 / 흐림 노드 |
| --- | --- | --- | --- |
| 기본 | null | mini 6 | — / 0 |
| 카드 위 호버 | null | mini 6 | 5 / 16 (카드 5장도 `opacity .16`) |
| **카드 클릭** | `kr-pol-20260810-1` | full 1 + mini 3 | **5 / 16** |
| **노드 클릭** | `kr-pol-20260810-1` | full 1 + mini 3 | **5 / 16** |

커서를 빈 곳으로 옮긴 뒤 두 경로의 상태가 `pinned`·`driver`·카드 수·강조 수까지 **완전히 일치**한다.
호버 시 테두리는 `rgb(236,48,19)`로 바뀐다. JS 오류 0건.

4개 폭(1440/768/390/360) 회귀 검사도 세션 (10)과 동일 — 영역 이탈 0, 겹침 0.

### 알아둘 것

`draw()`의 우선순위가 `hover || pin`이라, **카드를 클릭한 직후 커서가 재배치된 이웃 카드 위에 얹히면
잠깐 그 이웃의 클러스터가 미리보기로 뜬다.** 커서를 움직이면 바로 핀 강조로 돌아온다.
원저자가 정한 우선순위(핀 상태에서도 호버로 다른 클러스터를 미리 본다)를 그대로 둔 결과다.

---

## 2026-08-10 (10) — 좁은 화면 카드 정책: 하단 도킹 1장 (세션 9 결함 해소)

세션 (9)에서 제시한 3안 중 **2안(좁은 화면은 탭했을 때만 카드)** 을 사용자가 택했다.
**70줄 추가 / 18줄 수정.**

### 왜 '측면 거터'가 아니라 '하단 도킹'인가

2안을 원안대로 구현하면 카드가 여전히 오른쪽 거터에 선다. 그런데 계산해보면 안 된다.

```
390px 화면 → 그래프 폭 354px
full 카드 230px + 여백 → 노드가 설 자리 80px
```

`fitStep`의 `Math.max(80, ...)` 하한에 그대로 걸린다. 즉 카드를 띄우는 순간
노드 구름이 폭 80px 띠로 짜부라진다. 그래서 좁은 화면에서는 **카드를 하단에 가로로 눕히고**
(`side: 'B'`, 폭 `this.w - 20`) `fitStep`이 그만큼 **아래쪽만** 비우도록 했다.
노드는 가로 폭을 온전히 쓰고 카드에 가려지지 않는다.

### 적용

- **`narrow` / `activePin` / `cardsOn` 게터 신설** — `cardsOn`은 넓은 화면에선 토글값 그대로,
  좁은 화면에선 **핀이 있을 때만 참**. 상시 카드가 사라지는 지점이 여기다
  - 게터 이름이 `pin`이면 기존 `pin(n)` 메서드와 충돌해 `this.pin(null)`이 깨진다.
    `activePin`으로 우회했다
- **`cardBudget()` 신설** — 좁은 화면 1장, 넓은 화면은 높이 기준 `floor((h-80)/140)*2`.
  핀 상태에서 이웃 카드는 **핀 노드와 가까운 순**으로 잘라낸다
- **`syncCards()`에 도킹 분기** — 좌우 열 계산을 건너뛰고 `tx:10 / ty:h-h_card-10`
- **`placeCards()` 분리** — 트윈·디밍 처리를 도킹/거터 양쪽이 공유
- **`fitStep` 패딩 재구성** — `room = this.cardsOn`.
  좁은 화면은 좌우 패딩을 카드 없을 때와 동일하게 두고 `padB`만 `_dockH + 26`으로 키운다
- `drawLeaders`는 도킹 카드(`side === 'B'`)를 건너뛴다 — 가로로 누운 카드에 리더선은 화면만 가른다
- `cardLayer`에 `overflow:hidden` — **호스트에 이미 걸려 있어 사실상 중복이다.**
  카드 레이어 자체에 명시해 두는 의미만 있고, 동작은 이 줄이 없어도 같다
- **부수 수정 2건**
  - `pin(null)` 후 안내 문구가 돌아오지 않던 버그 — `pin()`이 `syncToolbar()`를 부르게 했다
  - 안내 문구가 폭에 따라 바뀌도록(`클릭` / `탭`) `resize()`에서 `syncToolbar()` 호출

### 검증 — 헤드리스 Chrome, 4개 폭 × 핀 전후

| 폭 | 그래프 | narrow | 핀 전 카드 | 핀 후 카드 | 겹침 | **영역 이탈** | 카드에 가린 노드 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1440px | 1176×600 | false | 6 | 4 | 0 | **0** | 0 / 21 |
| 768px | 717×600 | true | **0** | **1** | 0 | **0** | 0 / 21 |
| 390px | 354×600 | true | **0** | **1** | 0 | **0** | 0 / 21 |
| 360px | 324×600 | true | **0** | **1** | 0 | **0** | 0 / 21 |

- 세션 (9)의 **3장 이탈 → 0장**. 가로 넘침도 전 폭에서 없음 (`scrollWidth == vw`)
- 데스크탑 수치는 세션 (9)와 동일 — **회귀 없음**
- 토글 `그래프만` → 0장, 되돌리면 6장 복원. JS 오류·예외 0건
- `nodesUnderCards 0/21` — 패딩 예약이 실제로 노드를 카드 밖으로 밀어낸다

> 임계값은 **뷰포트가 아니라 그래프 엘리먼트 폭** 기준(`this.w <= 720`)이다.
> 그래서 뷰포트 768px가 `narrow`로 잡힌다(그래프는 717px). 여백을 기준으로 삼는 게 맞아 그대로 뒀다.

### 다음 할 일

세션 (8) 항목 그대로 — 내일 06:40 KST 첫 자동 수집 검증.

---

## 2026-08-10 (9) — 관계도 요약 카드 레이어 반영 (외부 수정분)

사용자가 claude.ai/design 쪽에서 고친 `issue-graph.js`를
`claude-design-변경사항/` 폴더로 넘겨줘서 그대로 반영했다. **220줄 추가 / 8줄 수정.**

### 무엇이 바뀌었나

캔버스 위에 `cardLayer`(z-index 9)를 얹어 노드 옆에 요약 카드를 세운다.

- **기본 상태**: 오늘 핵심(`kind === 'core'`) 노드마다 mini 카드
- **노드 클릭**: 해당 노드가 full 카드로 펼쳐지고 연결된 이슈만 mini로 남는다 (`pin`)
  — `summaries`가 켜져 있으면 클릭이 `issuejump` 대신 `pin`으로 간다.
  full 카드 안의 '요약으로 이동' 버튼이 기존 `issuejump` 역할을 대신한다
- **배치**: 카드는 좌우 여백(gutter)에만 서고 노드 구름 위로는 안 올라간다.
  한쪽 열이 넘치면 `balance()`가 가장 중앙에 가까운 카드를 반대편으로 넘긴다.
  노드와 카드는 점선 리더(`drawLeaders`)로 잇는다
- **끄기**: 좌상단 툴바('관계 요약' / '그래프만')와 설정 패널 체크박스 두 곳
- `fitStep`의 패딩이 `summaries` 상태에 따라 달라진다 — 카드 설 자리를 미리 비워둔다
- **팔레트 반전**: `level`이 hub `#f3f2f2` / core `#ec3013` / past `#6b6866`로 바뀌었다
  (기존은 hub가 강조색이었다)

### 검증 — 헤드리스 Chrome (CDP 직접 구동)

`node --check` 통과. 실제 페이지를 띄워 카드 위치를 실측했다.

| 상황 | 카드 | 겹침 | 영역 이탈 | 콘솔 오류 |
| --- | --- | --- | --- | --- |
| 1440px 기본 | 6장 (core 6개) | 0 | 0 | 없음 |
| `setSummaries(false)` | 0장 (DOM에서 제거) | — | — | 없음 |
| core 노드 pin | 4장 (full 1 + mini 3) | 0 | 0 | 없음 |
| **390px 모바일** | 6장 | 0 | **3장 이탈** ❌ | 없음 |

`404`는 `favicon.ico` 하나뿐 — 기존부터 있던 것이고 이 변경과 무관하다.
페이지 가로 넘침도 없다 (`scrollWidth 390 = vw`, 세션 5~7 대응분 회귀 없음).

### 남은 결함 — 390px에서 카드 3장이 그래프 밖으로 흘러넘친다

`wideCols`(`w > 720`) 판정 때문에 좁은 화면에서는 **카드가 전부 오른쪽 한 열로 쌓인다.**

```
그래프 높이 600px  vs  카드 6장 스택 919px  →  319px 초과
실측 bottom 값: 667 / 824 / 981  (모두 600 초과)
```

넘친 카드는 **호스트의 `overflow:hidden`(`issue-graph.js:54`)에 잘려 보이지 않는다.**
페이지 아래 콘텐츠를 덮지는 않으니 레이아웃은 안전하지만, 카드 절반이 그냥 사라진다.

> 정정: 이 항목은 처음에 "호스트에 `overflow:hidden`이 없어 아래 콘텐츠 위로 겹쳐 나온다"고
> 적었으나 **사실이 아니다.** 확인 명령이 `head -5`에 잘려 53번째 줄의 `overflow:hidden`을
> 놓친 탓이다. 결함의 실체(카드 3장이 안 보인다)와 조치는 그대로 유효하다.

고칠 방법은 세 가지가 있고, 어느 쪽이든 사용자 판단이 필요해 손대지 않았다.

1. 좁은 화면에서는 `cardTargets()`가 카드 수를 제한 (예: core 상위 3개만)
2. 좁은 화면에서는 `summaries` 기본값을 `false`로 — 클릭했을 때만 pin 카드 1~2장
3. 카드가 넘칠 때 `cardLayer`에 세로 스크롤 허용

### 후속 조치 — 2안 채택 (같은 세션에서 이어서 처리, 위 세션 (10) 참조)

---

## 2026-08-10 (8) — cron 활성화, 자동 운영 개시

`.github/workflows/daily-briefing.yml`의 `schedule` 블록 주석을 풀었다.

```yaml
on:
  workflow_dispatch:
  schedule:
    - cron: "40 21 * * *"   # UTC 21:40 = KST 06:40 (다음 날)
```

**첫 자동 실행: 2026-08-11 06:40 KST.**

이 실행이 곧 3단계 개편 지시서의 첫 실전 검증이기도 하다
(오늘자 데이터가 이미 있어 수동 검증이 불가능했다 — 0단계 규칙상 전 국가가 건너뛰어진다).

### 다음 날 아침 확인할 것

1. `git pull` 후 `data/issues.json`에 `2026.08.11` 기사 24건이 들어왔는지
2. **`related` 24/24** — 3단계 지시서 개편의 핵심 검증 항목
3. **`유사` 비율이 절반 이하**인지 (직전 실행은 15/19로 79%였다)
4. 모든 `updatedAt`이 `KST`로 끝나는지 (직전 실행은 EST·CST·JST 혼용)
5. `sources`가 기사당 1건인지
6. 실행 로그 아티팩트(`claude-execution-output`, 14일 보관)로 2단계가
   실제로 별도 탐색을 했는지 확인 — 이번부터 추측 없이 볼 수 있다

### 실패했을 때의 양상 (데이터는 안전하다)

- 지시서 검증 실패 → 저장하지 않고 종료. 어제 데이터가 그대로 남는다
- 푸시 실패 → 워크플로만 실패. 리포는 변경 없음
- 폭주 → `timeout-minutes: 45`에서 끊긴다

### 알아둘 것

GitHub는 **60일간 리포 활동이 없으면 예약 워크플로를 자동 비활성화**한다.
매일 커밋이 쌓이는 구조라 실제로 걸릴 일은 없지만, 수집이 오래 멈추면 이것부터 확인할 것.

---

## 2026-08-10 (7) — 지구본 국가 칩 바 모바일 대응

### 문제

하단 국가 칩 바가 `repeat(12,1fr)` 고정이라 390px에서 칸당 32px밖에 안 됐다.
국가명이 `대`, `미`, `중`처럼 **한 글자로 잘려** 어느 나라인지 알 수 없었다.

### 열 수를 정한 근거

가장 긴 이름이 `우크라이나`(5자)다. 12px 기준 약 60px + 좌우 패딩이 필요하다.

| 배치 | 390px 기준 칸 너비 | 판정 |
| --- | --- | --- |
| 6열 × 2행 | 65px (내용 45px) | ❌ `우크라…`로 잘림 |
| **4열 × 3행** | **98px (내용 82px)** | ✅ 전부 표시 |

가로 스크롤 방식도 후보였으나, 12개 고정 항목의 주 내비게이션이라
**한 화면에 전부 보이는 편이 낫다**고 판단해 4×3을 택했다.

### 적용

- `.globe-chips{grid-template-columns:repeat(4,1fr)}`
- `.globe-chip` 패딩 축소(`11px 10px` → `9px 8px`), 행 구분용 `border-bottom` 추가
- 국가 코드 10px → 9px
- **`.globe-cue`를 `bottom:96px` → `152px`로 올림** — 칩 바가 49px → 133px로 높아져
  그대로 두면 스크롤 안내 문구가 칩 바에 깔린다
- `.globe-cue`의 `left:50%;transform:translateX(-50%)`를 `left:0;right:0`로 교체 —
  포함 블록이 50%로 잡혀 안내 문구가 두 줄로 깨지던 문제도 함께 해결

### 검증 — 헤드리스 Chrome

각 칩의 이름 `scrollWidth`와 `clientWidth`를 비교해 **실제 잘림 여부**를 쟀다.

| 폭 | 배치 | 칩 너비 | 잘린 이름 | 안내문구·칩바 겹침 |
| --- | --- | --- | --- | --- |
| 360px | 4열 3행 | 90px | **0개** | 없음 |
| 390px | 4열 3행 | 98px | **0개** | 없음 |
| 430px | 4열 3행 | 108px | **0개** | 없음 |
| 1280px | 12열 1행 | 107px | 0개 | 없음 (회귀 없음) |

360px에서도 `우크라이나`가 온전히 들어간다.

### 남은 것

- 뉴스레터 블록 (`font-size:38px`, `padding:56px 40px`) — 유일하게 남은 데스크탑 고정 비율 구간

---

## 2026-08-10 (6) — 표 모바일 스택, 대륙 필터·날짜 박스 제거

### 1. '오늘의 주요 이슈' 표 — 모바일에서 카드로 접기

3열 테이블(분야 / Why / So What)이 모바일에서 열이 눌려 읽기 어려웠다.
`thead`를 숨기고 각 행을 카드 한 장으로 세우되, **각 칸이 `data-label`을 제 라벨로 띄우게** 했다.
라벨 없이 그냥 쌓기만 하면 어느 쪽이 Why고 어느 쪽이 So What인지 알 수 없다.

```
[정치 1]                  ← 분야 태그
WHY · 배경 원인
첨단산업 인력난과 …
│ SO WHAT · 향후 영향     ← 강조색 좌측 보더로 구분
│ 부처 간 이견이 …
```

- `.issue-table thead{display:none}` / `tr,td{display:block}`
- `td[data-label]::before{content:attr(data-label)}` — 라벨 자동 생성
- `.c-sowhat`에 `border-left:2px solid #ec3013` — Why와 So What을 눈으로 구분

데스크탑은 그대로다 (1280px 실측: `thead` 표시, 3열 120/506/550px 나란히).

### 2. 지구본 대륙 필터 제거

좌측 세로 버튼 7개(전체/아시아/북미/유럽/중동/남미/오세아니아)를 걷어냈다.

- 마크업 블록 삭제
- `<globe-stage>`의 `region` 속성 제거 — `globe.js:64`가 속성 부재 시 `'All'`로 기본 처리하므로 안전
- `state.region`, `regionDefs`, `regions` 제거
- 칩 흐리기 로직(`dim`) 제거 → `opacity:'1'` 고정

### 3. 우측 상단 날짜 박스 제거

- 마크업 블록과 `date` / `dates` / `onDate` / `state.date` 제거
- **부수 효과로 하드코딩 버그를 하나 걷어냈다** — `dates`가
  `['2026.08.09','2026.08.08','2026.08.07']`로 박혀 있어 이미 오늘 날짜를 못 담고 있었다
- 남은 `{{ date }}` 라벨(`KR · Daily Briefing · …`)은 데이터에서 파생하도록 교체:
  `latestDate(d)` 헬퍼 신설 → `COUNTRIES`의 `latestDate` 중 최댓값
- 빈 브리핑 안내문에서도 고정 날짜를 빼 스스로 갱신되게 했다

### 검증 — 헤드리스 Chrome

| 항목 | 결과 |
| --- | --- |
| 미해결 템플릿 변수 (`.sc-unresolved`/`.sc-missing`) | 0 |
| 로직 오류 (`.sc-logic-error`) | 0 |
| 콘솔 오류·예외 | 없음 |
| 대륙 필터 버튼 | 0개 |
| 날짜 `<select>` | 0개 |
| 390px 가로 넘침 | 없음 (`scrollWidth 390 = vw`) |
| 1280px 표 레이아웃 | 3열 유지, 회귀 없음 |

### 손대지 않은 곳

- **지구본 하단 국가 칩 바** — `repeat(12,1fr)`이라 390px에서 칸당 32px,
  국가명이 한 글자로 잘린다 (`대`, `미`, `중` …)
- 뉴스레터 블록 (`font-size:38px`, `padding:56px 40px`)

---

## 2026-08-10 (5) — '분야별 주요 소식 및 맥락 분석' 모바일 레이아웃 수정

### 원인 — 프로젝트 전체에 미디어 쿼리가 하나도 없었다

`Globe Briefing.dc.html`, `_ds/*/styles.css` 어디에도 `@media` 규칙이 없었다.
데스크탑 고정 폭 레이아웃이 모바일에서 그대로 렌더링되고 있었다.

390px 화면 기준 실측 (컨테이너 좌우 패딩 32px씩 제외 → 가용 폭 326px):

| 요소 | 문제 |
| --- | --- |
| 탭 바 (정치/경제/기타) | `padding:16px 26px` 3개 ≈ 351px → **가로 스크롤 발생** |
| 기사 카드 | `grid-template-columns:150px 1fr` + `padding-left:30px` → 본문 열이 **약 118px** |
| 맥락 패널 | `grid-template-columns:170px 1fr` → 118px 칸 안에서 또 넘침 |
| 섹션 제목 | 한 줄에 안 들어가는데 `flex-wrap` 없음 |
| 우하단 고정 버튼 | 58×58px가 '출처' 링크와 관련기사 박스를 덮음 |

### 적용한 수정 — `Globe Briefing.dc.html`

이 화면은 전부 인라인 `style`로 짜여 있어 선택자 특이도로는 못 이긴다.
**훅 클래스 + `@media (max-width:768px)` + `!important`** 조합으로 처리했다.
(`support.js:436`에서 `class` → `className` 변환이 되므로 클래스가 유지된다)

- `.briefing-wrap` 좌우 패딩 32px → 18px
- `.sec-head` 줄바꿈 허용, 제목 15px → 13px
- `.stream-tabs` / `.stream-tab` 전체 폭 3등분, 패딩 축소
- `.stream-orders` 아래 줄로 분리
- `.stream-item` 1열로 전환, 날짜·분류·번호는 가로 한 줄로 접음
- `.stream-title` 25px → 20px
- `.stream-panel` 1열로 전환 (라벨 위 / 본문 아래)
- `.stream-relgrid` 1열로 전환
- `.fab` 58px → 44px, 구석으로 이동 (본문 가림 해소)
- 본문 열에 `min-width:0` 추가 — grid 자식의 기본 `min-width:auto`가 축소를 막고 있었다

### 검증 — 헤드리스 Chrome으로 실측

`puppeteer` 설치 없이 Chrome을 CDP로 직접 구동해 `document.scrollWidth`를 쟀다.

| 폭 | 수정 전 | 수정 후 |
| --- | --- | --- |
| 360px | — | 360 ✅ |
| 390px | **477** ❌ (87px 넘침) | 390 ✅ |
| 430px | **477** ❌ (47px 넘침) | 430 ✅ |
| 768px | — | 768 ✅ |

넘침 요소도 10개 → 3개로 줄었고, 남은 3개는 모두 무해하다:
지구본 속보 마커 2개(`overflow:hidden` 섹션 내부)와 `aria-hidden` 장식용 div 1개.

### 손대지 않은 곳

같은 문제를 안고 있으나 이번 요청 범위 밖이라 그대로 둔 구간:

- **'오늘의 주요 이슈' 표** — `width:120px` 고정 열의 3열 테이블. 페이지 가로 스크롤은
  더 이상 없지만 모바일에서 열이 눌린다
- **뉴스레터 블록** — `font-size:38px`, `padding:56px 40px`
- **지구본 화면 상단 바** — `padding:22px 32px`의 좌우 배치

### 다음 할 일

1. 실제 기기에서 확인 (헤드리스 측정은 폰트 로딩·터치 영역까지 재현하지 못한다)
2. 위 세 구간도 같은 방식으로 처리할지 결정

---

## 2026-08-10 (4) — `related` 누락 원인 규명, 지시서 3단계 재구성

### 원인 — 탐색 실패가 아니라 배분 누락

`related`가 빈 5건이 왜 생겼는지 데이터에서 역상관이 나왔다.

```
related 있는 19건 →  sources 1개짜리 12건,  2개짜리 7건
related 없는  5건 →  전부 sources 2개
```

**5건 모두 두 번째 매체 기사를 찾아놓고 `related`로 올리는 대신 `sources` 배열에 넣어버렸다.**
예: `jp-pol-20260810-1`은 The Japan Times와 Nippon.com 두 건을 확보했으나 둘 다 출처로 처리.

근본 원인은 지시서의 **`sources` 개수 규칙 부재**였다. 필드 규칙 표에 `sources` 행 자체가 없어서
"두 번째로 찾은 기사를 어디로 보낼지"가 정의되지 않았다.

### 방증 — `related` type 분포가 뒤집혀 있었다

| | 반박 | 유사 | 다름 |
| --- | --- | --- | --- |
| 기존 (수기 48건) | **23** | 6 | 19 |
| 신규 (AI 19건) | **1** | **15** | 3 |

수기는 '반박'이 절반인데 AI는 1건. **반박 기사는 별도로 찾아야 나온다** —
AI가 한 번의 검색 결과에서 손에 잡히는 걸 골랐을 뿐 `related` 전용 탐색을 하지 않았다는 뜻이다.

### 적용한 수정

**`scripts/collect-prompt.md` — 3단계 구조로 재구성** (사용자 제안)

```
0단계 기존 파일 읽기 → 1단계 이슈 탐색 → 2단계 related 확보 → 3단계 정제 및 저장
```

- **1단계**: 검색 결과에서 대표 기사 1건만 `sources`에 넣고, **나머지는 버리지 말고 2단계로 넘긴다**
- **2단계**: ① 1단계 잔여 후보를 먼저 배정 → ② 없을 때만 `"<핵심어> 비판/반론"`으로 추가 검색
  → ③ 그래도 없으면 생략. **1단계 결과 재사용을 우선해 검색 비용 증가를 억제**하는 절충 설계
- **2단계**: `반박`·`다름` 우선, `유사`가 전체 절반을 넘지 않도록 명시
- **필드 규칙에 `sources` 행 신설** — 기사당 1건 원칙, 두 번째 기사는 `related`로
- **검증 체크리스트 4개 항목 추가** — `sources` 개수 / `related` 전건 존재 / `유사` 비율 / `updatedAt` KST
- `updatedAt` 4개국 KST 통일 명시 (지난 실행에서 EST·CST·JST 혼용된 문제)

**`.github/workflows/daily-briefing.yml` — 실행 로그 아티팩트 업로드 추가**

이번 원인 분석에서 턴 단위 기록이 없어 데이터 역산에 의존해야 했다.
`claude-execution-output.json`은 러너의 `_temp`에만 남고 폐기된다(`artifacts: 0`).
`actions/upload-artifact@v4`로 14일 보관하도록 추가 — 다음부터는 추측 없이 확인 가능하다.

### 다음 할 일

1. 수정된 지시서로 재실행해 `related` 24/24 및 `유사` 비율 개선 확인
   — 단, 오늘자(`2026.08.10`) 데이터가 이미 있어 0단계 규칙상 **전 국가가 건너뛰어진다.**
   내일 실행하거나, 검증하려면 임시로 `latestDate`를 되돌려야 한다
2. 기존 24건의 `related` 누락 5건은 수기 보완 또는 그대로 두기
3. cron 활성화

---

## 2026-08-10 (3) — 수정 검증 완료: **파이프라인 정상 동작 확인**

### 결론

**푸시까지 통과했다. 자동 수집 파이프라인이 처음으로 끝까지 성공했다.**

### 실행 결과 — run `31363178855`

| 항목 | 값 |
| --- | --- |
| 결론 | ✅ **success** |
| 소요 시간 | 13분 9초 (06:45:22Z → 06:58:31Z) — 이전 24분에서 단축 |
| Claude 실행 | `is_error: false`, `num_turns: 51`, `total_cost_usd: **$5.98**` |
| 커밋 | ✅ `a42b974 브리핑 2026.08.10` (866 insertions, 12 deletions) |
| 푸시 | ✅ `9158d8f..a42b974  HEAD -> main` |

### 데이터 검증 (로컬 pull 후 스크립트 검사)

| 검사 항목 | 결과 |
| --- | --- |
| 4개국 `latestDate` | ✅ 전부 `2026.08.10` |
| 국가별 신규 기사 | ✅ 6건씩 (정치 2 / 경제 2 / 기타 2), 총 24건 |
| 누적 기사 수 | 국가당 12건 → **18건** |
| `id` 중복 | ✅ 없음 |
| 필수 필드 누락 (`context` 4개 포함) | ✅ 없음 |
| `coreQuestion`이 `~까요?`로 끝나는가 | ✅ 24/24 통과 |
| `review: "pending"` | ✅ 24/24 |
| `period` / `updatedAt` 갱신 | ✅ 됨 |

### 그래프 연결성 — 가장 중요했던 리스크, 통과

`issue-graph.js`의 `similarity()`(topics 가중 자카드 + 동일 카테고리 시 0.08, 임계값 0.05)를
그대로 재현해 검사했다.

> **과거 기사와 연결이 하나도 없는 신규 노드: 0 / 24**

`PIPELINE.md`가 경고했던 "새 점이 고립되는" 문제는 발생하지 않았다.
지시서의 topics 재사용 규칙이 실제로 작동한다.

### 남은 흠 두 가지 (동작에는 지장 없음)

1. **`related` 누락 5건** — `kr-eco-20260810-2`, `kr-oth-20260810-2`, `jp-pol-20260810-1`,
   `jp-pol-20260810-2`, `jp-oth-20260810-1`.
   화면에서 '함께 읽으면 좋은 기사' 블록이 비어 보인다.
   지시서의 저장 전 검증 체크리스트에 `related` 항목이 없어서 걸러지지 않았다.
2. **`updatedAt` 타임존 불일치** — 지시서는 `"오늘날짜 08:00 KST"`로 고정하라고 했으나
   실제로는 US=`EST`, CN=`CST`, JP=`JST`로 각국 현지 시간대를 썼다. KR만 `KST`.

### 다음 할 일

1. 화면에서 실제 렌더링 확인 — `python -m http.server 8000` 후 타임라인·관계도 육안 검수
2. 24건 검수 후 `review`를 `"ok"`로 변경
3. 위 흠 2건을 고칠지 결정 — 고친다면 `scripts/collect-prompt.md`의 검증 체크리스트에
   `related` 존재 여부와 `updatedAt` 타임존 고정을 추가
4. **cron 활성화** — 2회 실측 24분/88턴, 13분/51턴.
   `total_cost_usd`(명목 $9.14 / $5.98)는 API 정가 환산값이지 청구액이 아니다.
   구독(Max 플랜) 쿼터로 도는 만큼 이 규모는 문제되지 않는다

---

## 2026-08-10 (2) — 워크플로 재실행: 수집 성공, **푸시 인증 실패** 원인 규명 및 수정

### 결론

**수집 로직은 정상이었다. 문제는 마지막 `git push` 단계의 인증이었다.**

### 실행 결과 — run `31361075139`

| 항목 | 값 |
| --- | --- |
| 소요 시간 | **24분** (06:11:55Z → 06:36:10Z) |
| Claude 실행 | `is_error: false`, `num_turns: 88`, `total_cost_usd: **$9.14**` |
| 데이터 변경 | ✅ `data/issues.json` **897 insertions, 12 deletions** |
| 로컬 커밋 | ✅ `bb24423 브리핑 2026.08.10` |
| 푸시 | ❌ `remote: Invalid username or token` → exit 128 |
| 최종 결론 | **failure** — 러너가 폐기되며 수집한 데이터도 함께 사라졌다 |

> 12 deletions는 4개국 × (`latestDate`/`period`/`updatedAt`) = 12줄 교체와 정확히 일치한다.
> 지시서대로 동작했다는 뜻이다.

### 원인

로그 타임라인이 원인을 그대로 보여준다.

```
06:12:00  actions/checkout@v4 이 http.https://github.com/.extraheader 에 GITHUB_TOKEN 심음
06:12:06  claude-code-action: "Using GITHUB_TOKEN from OIDC" — 자체 앱 토큰을 OIDC로 발급
06:36:06.9  claude-code-action: "Revoke app token" — 스텝 종료하며 그 앱 토큰을 폐기
06:36:07.3  Commit result 스텝의 git push → 폐기된 토큰을 사용 → 401
```

즉 `claude-code-action`이 자신의 앱 토큰을 git 자격증명에 심어두고 스텝이 끝날 때 폐기하는데,
그 다음 스텝의 `git push`가 그 폐기된 자격증명을 그대로 집어 든다.
**워크플로 자체의 `GITHUB_TOKEN`은 멀쩡했지만 쓰이지 않았다.**

### 적용한 수정 — `.github/workflows/daily-briefing.yml`

1. `Commit result` 스텝에 `env: GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` 추가
2. 푸시 직전에 오염된 자격증명을 제거하고 워크플로 토큰으로 명시적으로 푸시

```bash
git config --local --unset-all http.https://github.com/.extraheader || true
git push "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main
```

3. 폭주 방지용 `timeout-minutes: 45` 추가 (실측 24분 기준)

### 실행 규모 참고 — `total_cost_usd`는 청구액이 아니다

로그의 `total_cost_usd: 9.14`는 **API 정가로 환산한 명목값**이다.
이 워크플로는 `claude_code_oauth_token`(구독 토큰)으로 도는 만큼 **별도 API 청구는 없다.**
`PIPELINE.md`의 서술("별도 API 과금 없음, 다만 구독 쿼터를 함께 소모")이 정확하다.

실제 제약은 금액이 아니라 **구독 쿼터**이고, 사용자는 Max 플랜이라 이 규모는 부담이 되지 않는다.
따라서 아래 절감안은 **당장 적용할 이유가 없다.** 나중에 국가를 크게 늘릴 때만 다시 볼 것:

- 국가 수 축소 (`scripts/collect-prompt.md`의 '대상')
- 국가당 건수 축소
- `related` 조건 완화 — 기사당 검색이 늘어나는 주 요인
- 매일 → 격일

기록해 둘 값: **1회 실행 24분 / 88턴** (명목 $9.14).

### 다음 할 일

1. 수정된 워크플로로 **재실행해 푸시까지 통과하는지 확인** (~24분)
2. 통과하면 그래프에서 새 노드가 기존 노드와 이어지는지 확인
3. cron 활성화

---

## 2026-08-10 (1) — 로컬 개발 환경 연결, 파이프라인 현황 점검

### 한 일

- `C:\Users\user\Documents\global-daily-briefing`에 리포를 클론해 로컬 작업 폴더로 지정했다.
  - `origin` = `https://github.com/easilyidentified/global-daily-briefing.git`, 브랜치 `main`
  - `gh` CLI가 `easilyidentified` 계정으로 이미 인증되어 있어 push/PR 바로 가능
- 리포 전체를 읽고 프로젝트 맥락 문서 `CLAUDE.md`를 새로 작성했다.
- 세션 진척 로그(이 파일)를 만들었다.

### 확인한 사실

**자동화 파이프라인 — 절반만 동작 중**

| 항목 | 상태 |
| --- | --- |
| 리포 시크릿 `CLAUDE_CODE_OAUTH_TOKEN` | ✅ 등록됨 (2026-08-10 05:48 UTC) |
| GitHub Pages 배포 | ✅ 성공 (run `31360041320`) |
| 워크플로 수동 실행 | ⚠️ 2회 실패 후 3회차 성공 — 그러나 **데이터가 갱신되지 않음** |
| cron 자동 실행 | ❌ 아직 주석 처리 상태 |

- 실패한 실행: `31359446040`(28s), `31359570208`(31s) — 둘 다 시크릿 등록 이전 시각이라 인증 문제로 보인다.
- 성공한 실행: `31359799043` (05:49 UTC, 2m25s)
  - `is_error: false`, `num_turns: 9`, `total_cost_usd: 1.35` — 액션 자체는 정상 완료
  - 그러나 커밋 스텝 로그가 **"변경 없음 — 커밋하지 않습니다."**
  - 즉 Claude가 `data/issues.json`을 건드리지 않았다
  - 이 실행이 체크아웃한 커밋은 `02667638`이고, 현재 HEAD인 `1031c57`("Add files via upload")은 05:53 —
    **실행 이후에 사람이 파일을 업로드**했다. 실행 시점의 리포 상태가 지금과 달랐을 가능성이 있다.

**데이터 현황** — `data/issues.json`

- `SUPPORTED_COUNTRIES` 12개국 정의됨
- 실제 기사가 있는 국가는 4개: KR / US / CN / JP, 각각 12건
- 네 국가 모두 `latestDate` = `2026.08.09`
- `review: "pending"` 항목 0건
- → **오늘(2026.08.10) 브리핑은 아직 수집되지 않았다.**

### 다음 할 일

1. **워크플로를 현재 HEAD 기준으로 다시 수동 실행**해서 `data/issues.json`이 실제로 갱신되는지 확인
   `gh workflow run "Daily Briefing"` → `gh run watch`
2. 갱신되지 않으면 실행 로그에서 Claude의 실제 응답을 확인하고 `scripts/collect-prompt.md`를 조정
   (지시서가 "변경 불필요"로 판단하게 만드는 문구가 있는지 점검)
3. 결과가 정상이면 `.github/workflows/daily-briefing.yml`의 `schedule` 블록 주석을 풀어 **자동 실행 활성화**
4. 첫 자동 수집 후 그래프에서 **새 노드가 기존 노드와 연결되는지** 확인
   — 고립돼 있으면 지시서의 `topics` 재사용 규칙을 강화해야 한다
5. (선택) 화면에 `review: "pending"` 배지 표시 — `PIPELINE.md`에 후보로 적혀 있음

### 미해결 질문

- 성공한 실행이 데이터를 갱신하지 않은 원인이 **지시서 문제**인지 **체크아웃 시점 문제**인지 아직 구분되지 않았다.
  1번 항목(재실행)으로 판별 가능하다.
