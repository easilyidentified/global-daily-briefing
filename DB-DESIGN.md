# DB 구축 — 정의해야 할 항목과 권고안

> `ROADMAP.md`가 **왜** DB가 필요한지를 적는다면, 이 문서는 **무엇을 정해야 하는지**를 적는다.
> 결정이 내려지면 그 결정만 `ROADMAP.md`의 '굳어진 설계 결정'으로 옮긴다.
>
> 작성 시점 2026-08-12. 아직 **어느 것도 확정이 아니다.** 각 항목의 '권고'는 제안이다.

---

## 먼저 — 규모 때문이 아니다

설계를 시작하기 전에 전제를 바로잡는다. 실측한 값이다.

| | 값 |
| --- | --- |
| 지금 | 120건 / 248KB / 24일치 (2026.07.12~08.12) |
| 기사 1건 평균 | 1,674 B |
| 1년 후 (하루 24건) | 8,760건 / 약 14 MB |
| 3년 후 | 26,280건 / 약 42 MB |

**42MB는 어떤 DB에도 부담이 아니다.** 그러므로 "빅데이터라서", "느려서" DB를 도입하는 게 아니다.
이유는 셋뿐이고, 이 셋이 선택의 기준이 된다.

1. **쓰기** — 사용자 스크랩·계정·실시간 갱신은 쓰기 가능한 저장소 없이는 시작조차 안 된다
2. **관계 질의** — 온톨로지는 노드를 담는 게 아니라 관계를 묻는 것이다. JSON 파일로는 못 한다
3. **비공개** — `collect-prompt.md`와 축적 데이터가 public 리포에 열려 있다

이 셋을 못 하면 실패고, 이 셋만 하면 성공이다. **규모·성능은 판단 기준이 아니다.**

---

## 정의해야 할 항목 8가지

사용자가 꼽은 다섯(수집 데이터 / 정제 / 저장 / 통신 / 저장소)에 세 가지를 더했다.
빠뜨리면 나중에 반드시 되돌아와야 하는 것들이다.

| # | 항목 | 한 줄 질문 | 되돌리기 |
| --- | --- | --- | --- |
| 1 | **무엇을 저장하나** | 수집기가 무엇을 더 뱉어야 하나 | 비쌈 — 과거 재수집 |
| 2 | **어떻게 정제하나** | 표기가 다른 같은 것을 어떻게 묶나 | 중간 |
| 3 | **어떤 모양으로 저장하나** | 테이블을 어떻게 나누나 | **가장 비쌈** |
| 4 | **어떻게 꺼내나** | 프런트가 무엇을 어떻게 묻나 | 쌈 |
| 5 | **어디에 두나** | 어느 서비스에 올리나 | 중간 |
| 6 | **누가 무엇을 할 수 있나** | 권한을 어떻게 가르나 | 쌈 (단, 사고는 비쌈) |
| 7 | **어떻게 옮기나** | 매일 도는 파이프라인을 어떻게 안 깨나 | — |
| 8 | **어떻게 운영하나** | 백업·비용·고장 감지 | 쌈 |

> **3번이 가장 비싸다.** 테이블을 잘못 나누면 데이터를 다시 만들어야 한다.
> 세션 21의 "국가는 위계의 루트가 아니라 기사의 속성"이 바로 이 층의 결정이었다.

---

## 1. 무엇을 저장하나

### 지금 받고 있는 것

`title` `summary` `why` `soWhat` `context`4 `sources[]` `related` `topics[]` `date` `category` `country`

### 더 받아야 하는 것 — 온톨로지가 요구하는 최소치

| 추가 | 왜 필요한가 | 수집기 부담 |
| --- | --- | --- |
| **엔티티** (인물/조직/장소/정책·법안/지표) | `topics`는 문자열이라 `이재명`/`이 대통령`이 안 묶인다 | 지시서에 한 문단 |
| **근거 문장** (엔티티·관계마다) | 연결이 반박 가능해야 한다. 없으면 검증이 불가능 | 이미 읽은 본문에서 인용 |
| **주장 귀속** (`why`/`soWhat`이 누구 판단인가) | `반박` 관계가 실질적이려면 필요 | 지시서에 한 줄 |
| **사건 후보 이름** | 4개국이 같은 사건을 다룰 때 병합의 실마리 | 지시서에 한 줄 |

**권고 — 수집기는 "추출"만 하고 "판단"은 안 한다.**
사건 병합·이슈라인 연결은 DB 쪽 로직이 한다. 수집기에게 "이 사건이 어제 그 사건의 후속인가"를
물으면 매일 답이 흔들린다. 수집기는 **본문에 쓰여 있는 것만** 뽑는다.

> `flashes.json`의 "7월 7일 알레카야트에 이어 한 달 새 두 번째입니다" —
> 이 한 문장이 두 사건을 잇는 증거다. **이미 본문에 있고, 뽑지 않고 있을 뿐이다.**

### 원문을 버리지 않는다 (가장 중요한 원칙)

AI가 뱉은 **날것 그대로**를 따로 보관한다. 정규화된 표와 별개다.

```
수집기 출력(JSON 원문)  →  [보관: 영구]
                        └→  파싱 → 정규화 테이블들
```

이게 없으면 **관계 타입 하나 바꿀 때마다 4개국 과거를 다시 읽어야 한다.**
저장 비용은 하루 40KB로 무시할 수준이고, 얻는 것은 스키마를 언제든 다시 계산할 자유다.

---

## 2. 어떻게 정제하나

### 문제는 측정됐다

```
고유 topics          149개
1번만 쓰인 것         57개 (38%)   ← 아무것도 못 잇는다
통화정책 16회 / 금리 14회           ← 사실상 한 덩어리인데 따로 논다
```

### 권고 — 3단 정제

**① 별칭 사전 (alias → entity)**
`통화정책`·`금리`·`기준금리`를 하나의 엔티티에 붙인다. 사전은 사람이 만들지 않는다 —
**수집기가 "기존 목록에 있으면 그 단어를 쓰라"는 지시를 이미 받고 있고**(`collect-prompt.md`),
그걸 문자열 비교에서 엔티티 조회로 바꾸기만 하면 된다.

**② 흔함도(IDF) 가중**
`정부`·`국회`·`미국`은 거의 모든 기사에 나온다. 흔한 엔티티로 이으면 실뭉치가 된다.
`이 엔티티가 몇 %의 기사에 나오는가`를 계산해 두고, 흔할수록 연결 근거로서 값을 낮춘다.

> 지금 `issue-graph.js`의 `similarity()`가 가진 결함이 정확히 이것이다 —
> `반도체`와 `국회`를 똑같이 취급한다.

**③ 정규화는 나중에 다시 할 수 있게**
별칭 사전이 바뀌면 과거 데이터도 다시 묶어야 한다. **원문(1번)을 보관했으므로 가능하다.**
정규화 결과는 "다시 만들 수 있는 것"으로 취급하고, 원문은 "절대 못 만드는 것"으로 취급한다.

**하지 말 것 — 처음부터 완벽한 사전을 만들려 하지 않는다.**
엔티티 타입은 5개, 관계 타입은 6개 남짓으로 시작한다(세션 21 결정).
타입 **추가는 싸고 통합·분할은 비싸다** → 처음에 보수적으로, 적게.

---

## 3. 어떤 모양으로 저장하나 (스키마)

### 원칙 셋 — 세션 21에서 이미 굳힌 것

1. **국가는 기사의 속성이다.** `article.country` 컬럼. 위계의 루트가 아니다
2. **사건은 국가 무관하게 하나.** 4개국이 같은 사건을 다뤄도 `event`는 1행
3. **이슈라인은 노드가 아니라 경로.** `storyline` 테이블을 만들지 않는다. `event_link`를 잇다 보면 생긴다

### 권고 테이블

```
[보관 계층]
  raw_extraction    수집기 원문. 절대 안 지운다

[L0 — 지금 있는 것]
  article           기사. country/date/category/title/summary/why/soWhat/context
  article_source    기사의 출처 매체 (1~2건)
  article_related   함께 읽기 (유사/반박/다름)

[L1 — 엔티티]
  entity            인물/조직/장소/정책·법안/지표 + 정규화 이름
  entity_alias      표기 변형 → entity
  article_entity    기사↔엔티티 + 근거 문장

[L2 — 사건]
  event             사건. 국가 무관, 정본 이름
  article_event     기사는 사건의 '증거'로 붙는다

[L3 — 사건 사슬]
  event_link        event→event. 타입(선행/후속/인과/선례) + 근거 문장 필수

[L4 — 관점]
  claim             누가 무엇을 주장하는가. why/soWhat이 여기로 승격

[사용자]
  app_user / scrap / user_link    B2C-2 유료 기능. 사용자가 만든 연결
```

`L5 중요도`는 **테이블이 아니다.** 위 층에서 계산되는 파생값이라 뷰(view)로 둔다.
사람이 라벨을 붙이지 않는다.

### 왜 이 모양인가

- **`article`과 `event`를 나누는 것이 이 설계의 핵심이다.** 지금은 기사가 곧 노드라
  같은 사건을 4개국이 보도하면 **서로 무관한 노드 4개**가 된다.
  이 서비스의 최대 자산(4개국 동시 수집)이 지금 데이터에 안 잡히는 이유가 이것이다
- **`event_link`에 근거 문장을 필수로 둔다.** 근거 없는 연결은 검증할 수 없고,
  검증 못 하는 연결은 팔 수 없다
- **`claim`이 L4를 연다.** "같은 사건을 한·미·중·일이 어떻게 다르게 부르는가" —
  경쟁 서비스가 단일 언어권이라 만들 수 없는 것

### 하지 말 것

- **최상위 추상 층을 만들지 않는다.** "국가간 안보 경쟁" 같은 노드는 검증이 불가능해 철학이 된다
- **`한국 > 핫이슈 > 사건` 트리를 저장하지 않는다.** 핫이슈는 매일 바뀌므로 **질의 결과**여야 한다.
  저장 구조로 만들면 매일 트리를 다시 짜야 한다

---

## 4. 어떻게 꺼내나 (통신)

### 지금

```
브라우저 → fetch('data/issues.json') → 248KB 전량 다운로드 → JS가 필터링
```

**전량을 받아 클라이언트에서 거른다.** 3년 뒤 42MB를 매번 받게 된다.

### 권고

```
브라우저 → HTTPS API → 필요한 것만
   예) "KR의 최근 7일 기사"          → 42건
       "이 사건의 선행 사건 3단계"    → 재귀 질의 한 번
```

**권고 — 처음에는 DB가 자동 생성해주는 REST API를 쓴다.**
Supabase 계열은 테이블을 만들면 REST 엔드포인트가 자동으로 생긴다.
직접 서버를 짜지 않아도 되고, DB를 처음 하는 입장에서 **관리할 대상이 하나 줄어든다.**

**단, B2B-1(유료 API)은 이걸 그대로 팔지 않는다.** 자동 생성 API는 테이블 구조가 그대로 드러나
스키마를 바꾸는 순간 고객이 깨진다. 파는 API는 **별도의 버전 붙은 얇은 층**을 나중에 얹는다.
지금 만들 필요는 없다.

---

## 5. 어디에 두나 (저장소)

### 선택지 비교

| | 장점 | 단점 | 판단 |
| --- | --- | --- | --- |
| **Supabase** (관리형 Postgres) | Postgres + REST API + 로그인/계정 + 권한제어가 **한 묶음** | 서비스 종속 | **권고** |
| Neon 등 순수 관리형 Postgres | 더 싸고 가벼움 | API·로그인을 직접 만들어야 함 | 차선 |
| 자체 호스팅 (VPS + Postgres) | 완전한 통제 | 백업·보안·업데이트를 직접. **혼자서는 부담** | 비권고 |
| 그래프 DB (Neo4j 등) | 관계 질의에 특화 | 데이터가 작아 이점이 안 나옴. 운영 대상 하나 추가 | **지금은 비권고** |

### 권고 — 관리형 Postgres, 그중 Supabase

**이유 셋.**

1. **관계 질의가 된다.** Postgres의 재귀 질의로 "이 사건의 선행 → 그 선행 → ..." 사슬을 한 번에 캔다.
   지금 필요한 깊이(5~10단계)에는 충분하다
2. **로그인·권한이 딸려 온다.** B2C-2(스크랩·유료)는 계정이 전제인데, 그걸 직접 만들면
   보안 사고의 표면이 넓어진다. **비밀번호를 직접 다루지 않는 편이 안전하다**
3. **데이터가 작아서 무료~저가 구간에 오래 머문다.** 3년 42MB다

**그래프 DB는 나중에.** `ROADMAP.md`의 "Postgres로 시작해 부족하면 얹는다"가 맞다.
지금 얹으면 **운영 대상이 둘이 되는 비용만 치르고 이점은 못 얻는다.**

### GitHub Pages는 유지한다

```
public 리포   프런트만 (HTML/JS)      ← 지금처럼 Pages로 배포
비공개         DB + 수집 지시서 + 데이터
```

- 프런트는 어차피 브라우저에 다 보이므로 공개돼도 잃을 게 없다
- **`collect-prompt.md`가 최우선 분리 대상**이다. 코드가 아니라 이 지시서가 핵심 자산이다
- 이미 나간 히스토리는 되돌릴 수 없다. **늦을수록 손해가 커지는 유일한 항목이다**

---

## 6. 누가 무엇을 할 수 있나 (권한)

DB를 처음 하면 가장 사고가 잦은 곳이다. **키 하나가 새면 전부 새기 때문에** 처음부터 나눈다.

| 주체 | 읽기 | 쓰기 | 어떻게 |
| --- | --- | --- | --- |
| 방문자(비로그인) | 발행된 기사·사건만 | 없음 | 공개 키. **읽기 전용으로 못박는다** |
| 로그인 사용자 | 위 + 자기 스크랩 | 자기 스크랩만 | 행 단위 권한 |
| 수집기(Actions) | 전체 | 기사·사건 | **비밀 키. 절대 프런트에 넣지 않는다** |
| 나(운영) | 전체 | 전체 | 관리 콘솔 |

**핵심 한 줄 — 브라우저에 들어가는 키로는 절대 쓰기가 되면 안 된다.**
프런트 JS에 박힌 키는 누구나 볼 수 있다. 공개 키는 읽기 전용, 쓰기 키는 서버·Actions에만 둔다.

`review: "pending"`인 기사도 그대로 발행되므로(세션 22 결정) 별도 취급은 없다.

---

## 7. 어떻게 옮기나 (마이그레이션)

**매일 06:40 수집 / 08:00 발송이 돌고 있다. 이걸 멈추지 않고 옮겨야 한다.**

### 권고 — 5단계, 각 단계마다 되돌릴 수 있게

```
1  DB 만들고 기존 120건 이관        기존 파이프라인 그대로. 영향 없음
2  수집기가 DB에도 쓴다 (JSON도 계속) 둘 다 쓴다. 어긋나면 JSON이 정답
3  프런트를 API로 전환               문제 생기면 JSON 버전으로 되돌림
4  메일 스크립트를 API로 전환         "
5  JSON 생성 중단, 리포 정리          여기서부터 되돌리기 어려움
```

**2단계에서 한동안 나란히 돌린다.** 같은 데이터가 두 곳에 쌓이므로,
어긋나면 곧바로 보인다. 이 겹치는 기간이 안전장치다.

### 이관할 때 걸리는 것

- **id 형식이 두 가지다** (세션 23 기록). `kr-a-pol-1` 형태가 국가당 6건씩 24건.
  리포 어디서도 id를 쪼개 쓰지 않는 것은 확인했으므로, **DB에서도 쪼개지 않으면 그만이다.**
  국가·카테고리·날짜는 각자의 컬럼에서 읽는다 → **소급 수정 0건**
- **과거 기사 소급 수집이 따로 필요하다.** 지금 국가당 30건(약 5일치)로는
  '선례' 연결(2026 세제개편 ↔ 2020 세제개편)에 시간 깊이가 안 나온다.
  DB가 준비된 뒤 날짜만 바꿔 파이프라인을 돌리는 일회성 작업이다

### 스키마를 한 번에 크게 바꾸지 않는다

새 필드는 **전부 선택(optional)으로** 넣고, 화면은 없으면 없는 대로 돈다.
매일 도는 파이프라인이 있는 한 이 원칙을 어기면 그날 브리핑이 통째로 빈다.

---

## 8. 어떻게 운영하나

| 항목 | 권고 |
| --- | --- |
| **백업** | 관리형 서비스의 자동 백업 + **원문(raw_extraction) 주기적 내보내기**. 원문만 있으면 나머지는 다시 계산된다 |
| **비용** | 3년 42MB. 무료~저가 구간에 오래 머문다. 비용이 뛴다면 그건 데이터가 아니라 트래픽·기능 때문이다 |
| **고장 감지** | 지금은 수집 실패를 사람이 눈치채야 한다. **"오늘 기사 0건이면 알림"** 한 줄이 가장 값싼 감시다 |
| **검수** | 하지 않기로 결정됨(세션 22). `review: "pending"`이 정상 |

---

## 정하고 나면 이 순서로

```
저장소 결정  →  스키마 확정  →  DB 생성 + 120건 이관
   →  수집기 이중 쓰기  →  프런트 전환  →  메일 전환  →  리포 정리
```

**저장소부터다.** 어디에 두느냐가 API 방식과 권한 모델을 결정하고,
그 둘이 프런트 코드를 결정한다.

---

## 사용자 결정이 필요한 것

이 문서에서 내가 정할 수 없는 것들이다.

| # | 질문 | 내 권고 |
| --- | --- | --- |
| A | 저장소를 무엇으로 할까 | **Supabase** (관리형 Postgres + API + 로그인) |
| B | GitHub Pages 정적 배포를 유지할까 | **유지.** 프런트만 공개, 데이터·지시서는 분리 |
| C | `collect-prompt.md` 분리를 언제 할까 | **가장 먼저.** 시간이 지날수록 되돌릴 수 없는 유일한 항목 |
| D | 유료 기능(계정·스크랩)을 이번 스키마에 넣을까 | **자리만 잡고 지금 만들지는 않는다** |
| E | 과거 기사 소급 수집을 어디까지 | 선례 연결이 목적이면 **최소 1년**, 다만 수집 쿼터를 그만큼 쓴다 |
