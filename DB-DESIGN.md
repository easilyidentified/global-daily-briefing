# DB 구축 — 정의해야 할 항목과 권고안

> `ROADMAP.md`가 **왜** DB가 필요한지를 적는다면, 이 문서는 **무엇을 정해야 하는지**를 적는다.
> 결정이 내려지면 그 결정만 `ROADMAP.md`의 '굳어진 설계 결정'으로 옮긴다.
>
> 2026-08-12 작성. **v2 — 대규모 소급 수집 전제로 전면 개정.**
> v1은 "하루 24건이 계속된다"를 깔고 있어 항목 구성 자체가 틀렸다(아래 '무엇이 바뀌었나' 참조).

---

## 목적 (사용자 확인)

기사와 이슈는 **존재 자체가 절대적이지 않다.** 같은 사건을 두고 매체마다 사실 주장이 갈리고,
어느 쪽이 맞는지는 한 기사만 봐서는 정할 수 없다. 그것에 **신뢰도를 부여하는 것**이 온톨로지의 역할이고,
그러려면 **방대한 데이터가 필요하다** — 한 사건을 다룬 독립 출처가 여럿 있어야 비교가 성립한다.

이 목적이 설계 전체를 지배한다. 아래 모든 항목은 이 문장에서 파생된다.

---

## 무엇이 바뀌었나 (v1 → v2)

v1은 규모를 잘못 잡았고, 그 결과 **항목 구성이 틀렸다.**

| | v1 (틀림) | v2 |
| --- | --- | --- |
| 전제 | 하루 24건이 계속된다 | 과거를 방대하게 소급한다 |
| 3년 뒤 | 26,280건 / 42MB | 수백만~수천만 건 |
| 1순위 결정 | 저장소 | **획득과 추출 예산** |
| 빠져 있던 항목 | — | 획득 / 추출 예산 / 동일성 판정 / 신뢰도 계산 |

**저장소를 1순위로 물었던 것이 v1의 가장 큰 오류다.** 아래 실측이 그 이유다.

---

## 규모 재계산 — 저장은 여전히 병목이 아니다

기사 1건 심층 추출을 입력 2,000토큰 / 출력 600토큰으로 잡고, Batch API 50% 할인을 적용한 값이다.
**기사 전문은 저작권상 저장하지 않으므로**(`collect-prompt.md` 규칙) 용량은 메타데이터+요약 기준이다.

| 시나리오 | 기사 수 | 저장 용량 | AI 추출 1회 (Haiku) | (Sonnet) | (Opus) |
| --- | ---: | ---: | ---: | ---: | ---: |
| 현재 그대로 1년 | 8,760 | 0.02 GB | 3만원 | 9만원 | 15만원 |
| 4개국 5년 소급 | 36만 | 0.7 GB | 128만원 | 383만원 | 639만원 |
| **12개국 10년** | **438만** | **8.4 GB** | **1,533만원** | 4,599만원 | 7,665만원 |
| 12개국 20년 | 2,628만 | 50 GB | 9,198만원 | 2.8억원 | 4.6억원 |

**438만 건 기준으로 저장은 월 300원 수준, 추출은 1,533만원.**
비용 차이가 5만 배다. 그리고 50GB는 어떤 관계형 DB에도 작은 데이터다.

### 여기서 나오는 결론 셋

1. **"어디에 저장하느냐"는 1순위 결정이 아니다.** 어느 쪽을 골라도 감당한다
2. **1순위는 "어디서 가져오고, 무엇에 AI를 쓰느냐"다.** 여기서 비용이 100배 갈린다
3. **"일단 다 긁고 본다"가 가장 비싼 실수다.** 되돌릴 수 없는 돈이 나간다

> 사용자가 E(소급 범위)를 "온톨로지를 구축하며 필요한 만큼"으로 정한 것은
> **비용 구조상 정확히 옳다.** 추출이 지배적인 비용일 때 선긁기는 최악의 순서다.

---

## 정의해야 할 항목 — 4개 층, 11가지

v1의 8가지에서 넷을 더하고(획득 / 추출 예산 / 동일성 판정 / 신뢰도), 저장소의 순위를 내렸다.
**위 층이 아래 층을 결정한다.**

| 층 | # | 항목 | 한 줄 질문 | 되돌리기 |
| --- | --- | --- | --- | --- |
| **① 데이터를 얻는 법** | 1 | **획득** | 수백만 건을 어디서 가져오나 | **가장 비쌈** |
| | 2 | **추출 예산** | 무엇에 AI를 쓰고 무엇에 안 쓰나 | **가장 비쌈** |
| **② 데이터를 다루는 법** | 3 | 저장 대상 | 무엇을 남기고 무엇을 버리나 | 비쌈 |
| | 4 | **동일성 판정** | 같은 사건인지 어떻게 아나 | 비쌈 |
| | 5 | 정제 | 표기가 다른 같은 것을 어떻게 묶나 | 중간 |
| | 6 | **신뢰도 계산** | 무엇을 근거로 믿을만함을 매기나 | 중간 |
| **③ 두는 법** | 7 | 스키마 | 테이블을 어떻게 나누나 | 비쌈 |
| | 8 | 저장소 | 어느 서비스에 올리나 | 쌈 |
| | 9 | 질의·API | 프런트가 무엇을 묻나 | 쌈 |
| **④ 굴리는 법** | 10 | 권한 | 누가 무엇을 할 수 있나 | 쌈 (사고는 비쌈) |
| | 11 | 이관·운영 | 도는 파이프라인을 안 깨고 옮기기 | — |

**1·2번에 가장 오래 앉아 있어야 한다.** 잘못 정하면 되돌릴 수 없는 돈이 나간다.
스키마(7)는 v1에서 1등이었는데, 데이터를 다시 만드는 비용이 이제 훨씬 커졌으므로 여전히 상위권이다.

---

# 층 ① — 데이터를 얻는 법

## 1. 획득 — 수백만 건을 어디서 가져오나

**지금 방식은 이 규모로 못 간다.** 현재 수집기는 Claude Code 에이전트가 WebSearch로 하루 24건을 찾고,
1회 13~24분 / 51~88턴을 쓴다. 이 속도로 438만 건은 **500년**이 걸린다.

즉 **소급 수집은 지금 파이프라인의 날짜만 바꿔 돌리는 일이 아니다.**
(`ROADMAP.md`가 "날짜만 바꿔 돌리는 일회성 작업"이라고 적어둔 것은 이 규모를 가정하지 않은 서술이다)

### 선택지

| 경로 | 규모 | 깊이 | 비용 | 판단 |
| --- | --- | --- | --- | --- |
| **GDELT** | 2억 건+, 15분마다 갱신 | 얕음 (문장 단위 이벤트 코딩) | **무료** | **골격으로 쓴다** |
| 상용 뉴스 API | 중~대 | 중간 | 구독료 | 보조 |
| 직접 크롤링 | 무제한 | 원하는 대로 | 개발·운영·법적 부담 | 비권고 |
| 지금 방식 (AI 검색) | 하루 수십 건 | 깊음 | 구독 쿼터 | **오늘치 전용** |

**GDELT는 `ROADMAP.md`가 이미 조사해둔 것이다** — "규모는 압도적이나 문장 단위 얕은 추출,
엔티티 연결 약함". 그 평가는 맞고, 그래서 **골격으로만 쓴다.**

### 권고 — 2단 획득

```
넓고 얕게 (GDELT 등)   수백만 건의 뼈대: 언제·어디서·누가·어느 매체
                        ↓  여기서 "볼 만한 사건"을 고른다
좁고 깊게 (AI 추출)     선별된 사건만: 요약·why·soWhat·엔티티·근거 문장
```

**넓은 층이 신뢰도의 재료다.** 한 사건을 몇 개 매체가 어떻게 다뤘는지는 메타데이터만으로도 센다.
**깊은 층은 화면과 API에 나가는 것**이고, 여기에만 AI를 쓴다.

---

## 2. 추출 예산 — 무엇에 AI를 쓰나

**이것이 이 프로젝트에서 가장 비싼 단일 결정이다.** 위 표에서 100배가 갈린 곳이다.

### 지금 방식은 왜 안 되나

현재 수집은 **Max 구독 쿼터**로 돈다(`CLAUDE.md`: "별도 API 과금 없음").
구독 쿼터는 하루 24건에는 충분하지만 수백만 건에는 원리적으로 부족하다.
**대규모 소급을 하는 순간 API 종량 과금으로 넘어간다** — 이건 선택이 아니라 사실이다.

### 권고 — 3단 예산

| 층 | 대상 | 방법 | 438만 건 기준 비용 |
| --- | --- | --- | --- |
| **L-0 골격** | 전부 | GDELT 등 메타데이터, AI 안 씀 | **0원** |
| **L-1 선별 추출** | 사건이 될 만한 것 | 싼 모델 + **Batch API 50% 할인** | 전체의 5%만 하면 77만원 |
| **L-2 심층** | 화면·API에 나가는 것 | 좋은 모델 | 건수가 적어 무시할 수준 |

**핵심은 "전부에 AI를 돌리지 않는다"이다.** 438만 건 전수 추출은 1,533만원이지만,
5%만 골라 하면 77만원이다. 무엇을 고르느냐가 곧 예산이다.

### 반드시 쓸 것 — Batch API

즉시 응답이 필요 없는 소급 작업은 **Batch API로 정확히 절반**이다.
실시간이 필요 없는 작업에 실시간 요금을 내지 않는다.

### 모델 선택

| 작업 | 권고 | 이유 |
| --- | --- | --- |
| 대량 1차 추출 | **Haiku 4.5** ($1/$5) | 구조화 추출은 난도가 낮다. 가장 싸다 |
| 사건 병합·관계 판정 | Sonnet 5 ($3/$15) | 판단이 들어간다 |
| 오늘치 브리핑 | 지금 그대로 | 구독 쿼터로 충분 |

> 가격은 2026-06-24 기준. 실행 전 최신값을 확인할 것.

---

# 층 ② — 데이터를 다루는 법

## 3. 저장 대상 — 무엇을 남기나

### 반드시 지키는 두 줄

- **기사 전문은 저장하지 않는다.** 저작권 문제이자, 이 규칙 덕분에 20년치 12개국도 50GB에 들어간다
- **AI가 뱉은 날것을 영구 보관한다.** 정규화 결과와 별개다

두 번째가 **v1보다 훨씬 더 중요해졌다.** 규모가 커지면서 재추출 비용이 수천만원이 됐기 때문이다.

```
원문을 안 남기면 → 관계 타입 하나 바꿀 때마다 1,533만원
원문을 남기면   → 다시 계산만 하면 된다 (거의 0원)
```

원문 보관 비용은 위 표의 저장 용량 그대로다. **월 몇백원으로 수천만원을 보험 든다.**

---

## 4. 동일성 판정 — 같은 사건인지 어떻게 아나

**대규모에서 이것이 실질적인 최대 난제다.** v1에서는 스키마의 각주였지만, 수백만 건에서는 독립 항목이다.

`ROADMAP.md`가 이미 정확히 짚었다 — **"실시간의 진짜 난점은 수집이 아니라 중복 제거와 사건 병합이다."**
그 말이 소급 수집에도 똑같이 적용된다. 438만 건을 넣으면 같은 사건이 수십 개 노드로 쏟아진다.

### 왜 어려운가

- **문자열 비교로는 안 된다.** 같은 사건을 매체마다 다르게 부른다
- **전수 비교는 불가능하다.** 438만 건을 서로 비교하면 9조 쌍이다

### 권고 — 3단 좁히기

```
① 시간·엔티티로 후보를 좁힌다   같은 주 + 엔티티 공유 → 수십 건
② 벡터 유사도로 다시 좁힌다     임베딩 최근접 → 수 건
③ 그 몇 건만 AI가 판정한다      "같은 사건인가?" + 근거
```

**①②가 없으면 ③의 비용이 폭발한다.** 좁히기가 곧 예산 통제다.

**벡터 검색이 새로 필요해진다.** Postgres의 `pgvector` 확장으로 시작한다 —
저장소를 따로 늘리지 않아도 된다. 다만 **임베딩 모델은 별도 선택 사항**이다
(Anthropic API에는 임베딩 엔드포인트가 없다).

---

## 5. 정제 — 엔티티 정규화

현재 데이터에서 이미 측정된 문제다.

```
고유 topics 149개 중 57개(38%)가 딱 한 번만 쓰임  → 아무것도 못 잇는다
통화정책 16회 / 금리 14회                        → 사실상 한 덩어리인데 따로 논다
```

**규모가 커지면 이 문제도 커진다.** 438만 건에서 별칭이 안 묶이면 그래프가 통째로 무의미해진다.

### 권고 — 3단 정제

- **별칭 사전 (alias → entity)** — 수집기가 이미 "기존 목록에 있으면 그 단어를 쓰라"는 지시를 받고 있다.
  문자열 비교를 엔티티 조회로 바꾸면 된다
- **흔함도(IDF) 가중** — `정부`·`국회`·`미국`으로 이으면 실뭉치가 된다.
  *지금 `similarity()`의 결함이 정확히 이것 — `반도체`와 `국회`를 똑같이 취급한다*
- **다시 계산 가능하게** — 사전이 바뀌면 과거도 다시 묶는다. 원문(3번)이 있으므로 가능하다

**타입은 적게 시작한다.** 엔티티 5종·관계 6종 남짓. 추가는 싸고 통합·분할은 비싸다.

---

## 6. 신뢰도 계산 — 이 프로젝트의 목적

**사용자가 명시한 DB의 존재 이유다.** v1에는 `L5 중요도`의 각주로만 있었다.

### 무엇을 근거로 매기나

한 기사만 봐서는 못 정한다. **여러 출처를 겹쳐야 나온다.**

| 축 | 무엇을 보나 | 필요한 데이터 |
| --- | --- | --- |
| **출처 독립성** | 몇 개의 서로 무관한 매체가 다뤘나 | 넓은 층(GDELT)만으로 계산 가능 |
| **주장 일치도** | 같은 사실을 말하나, 갈리나 | 깊은 층(추출)이 필요 |
| **매체 이력** | 이 매체가 과거에 얼마나 맞았나 | **시간 깊이가 필요** ← 소급의 진짜 이유 |
| **지속성** | 정정·후속 보도가 있었나 | 시간축 연결 |
| **프레이밍 분산** | 4개국이 얼마나 다르게 부르나 | L4 관점 |

**여기가 소급 수집이 정당화되는 지점이다.** 매체 이력은 과거 없이는 계산 자체가 불가능하다.

### 권고

- **저장하는 값이 아니라 계산되는 값이다.** 사람이 라벨을 붙이지 않는다
- **근거를 함께 남긴다.** "왜 이 점수인가"를 못 보이면 팔 수 없다
- **처음에는 출처 독립성만으로 시작한다.** 넓은 층만으로 계산되고, 추출 비용이 0이다.
  나머지 축은 데이터가 쌓인 뒤 얹는다

---

# 층 ③ — 두는 법

## 7. 스키마

### 원칙 셋 — 세션 21에서 이미 굳힌 것

1. **국가는 기사의 속성이다.** `article.country` 컬럼. 위계의 루트가 아니다
2. **사건은 국가 무관하게 하나.** 4개국이 다뤄도 `event`는 1행
3. **이슈라인은 노드가 아니라 경로.** `storyline` 테이블을 만들지 않는다

### 권고 테이블

```
[보관]   raw_extraction     AI 원문. 영구. 재추출 비용이 수천만원이므로 절대 안 지운다
         source_bulk        GDELT 등 넓은 층 원본

[L0]     article            기사. country / date / category / why / soWhat
         article_source     출처 매체
         article_embedding  벡터 (동일성 판정용, pgvector)

[L1]     entity             인물 / 조직 / 장소 / 정책·법안 / 지표
         entity_alias       표기 변형 → entity
         article_entity     기사↔엔티티 + 근거 문장

[L2]     event              사건. 국가 무관
         article_event      기사는 사건의 '증거' + 병합 근거·확신도

[L3]     event_link         event→event. 타입 + 근거 문장 필수

[L4]     claim              누가 무엇을 주장하는가

[L5]     (뷰)               신뢰도·중요도 — 테이블이 아니라 계산값

[사용자]  app_user / scrap / user_link
```

`article_embedding`과 `source_bulk`가 v1에서 없던 것이다 — 규모 때문에 생겼다.

### 규모가 스키마에 주는 영향

438만 기사 × 엔티티 8개 = **3,500만 행**의 연결 테이블이 된다. Postgres에는 흔한 규모지만,
**인덱스 설계를 처음부터 해야 한다**(작아서 넘어가던 것이 안 넘어간다).

### 하지 말 것

- **최상위 추상 층을 만들지 않는다** — 검증 불가능해 철학이 된다
- **`한국 > 핫이슈 > 사건` 트리를 저장하지 않는다** — 질의 결과여야 한다

---

## 8. 저장소 — 순위가 내려간 결정

**규모를 다시 재봐도 결론이 크게 바뀌지 않는다.** 50GB / 수천만 행은 Postgres에 부담이 아니다.
바뀐 것은 **중요도**다 — 이건 이제 1순위 결정이 아니다.

### 권고 — 두 곳으로 나눈다

```
서빙용   관리형 Postgres     현재 데이터 · 그래프 · 계정 · API   ← 작고 자주 읽힘
보관용   객체 저장소(S3/R2)   AI 원문 · 넓은 층 원본            ← 크고 거의 안 읽힘
```

**나누는 이유는 성격이 다르기 때문이다.** 원문은 재계산할 때만 읽고, 서빙 데이터는 매 요청 읽힌다.
객체 저장소는 대용량이 훨씬 싸고, DB는 질의가 된다.

| 선택지 | 판단 |
| --- | --- |
| **관리형 Postgres + pgvector** | **권고.** 관계 질의 + 벡터 검색이 한 곳에서 된다 |
| 그래프 DB 추가 | **아직 아니다.** 재귀 질의로 충분하고, 운영 대상만 둘이 된다 |
| 분석 전용 저장소 추가 | **나중에.** 신뢰도 계산이 느려지면 그때 |

**어느 관리형 Postgres냐는 나중에 정해도 된다.** 스키마가 표준 SQL이면 옮기는 비용이 낮다.
계정·로그인이 필요하면 그 기능이 딸린 쪽(Supabase 등)이 편하고, 아니면 더 싼 쪽을 골라도 된다.
**이 결정을 1순위에서 뺀 것이 v2의 핵심 변경이다.**

### GitHub Pages는 유지 — 사용자 확인

> **"GitHub 정적 배포는 껍데기에 불과한 거지?"** — 맞다. DB 구축 후에는 그렇다.

```
지금        정적 페이지 안에 데이터가 들어 있다        ← 껍데기가 아니라 통째
바꾼 뒤     정적 페이지는 API를 부르는 껍데기          ← 껍데기가 맞다
```

프런트는 어차피 브라우저에 다 보이므로 공개돼도 잃을 게 없다.
**`collect-prompt.md`가 최우선 분리 대상**이고, **시간이 지날수록 되돌릴 수 없는 유일한 항목**이다.

---

## 9. 질의·API

지금은 브라우저가 `issues.json` 248KB를 통째로 받아 JS로 거른다.
438만 건이 되면 이 방식은 성립하지 않는다.

**권고** — 처음에는 DB가 자동 생성하는 REST API를 쓴다. 관리할 대상이 하나 준다.
**단, 유료 API(B2B-1)는 이걸 그대로 팔지 않는다** — 테이블 구조가 드러나 스키마를 바꾸면 고객이 깨진다.
버전 붙은 얇은 층을 나중에 얹는다.

---

# 층 ④ — 굴리는 법

## 10. 권한

DB를 처음 하면 가장 사고가 잦은 곳이다.

| 주체 | 읽기 | 쓰기 |
| --- | --- | --- |
| 방문자 | 발행된 기사·사건 | 없음 (공개 키는 **읽기 전용으로 못박음**) |
| 로그인 사용자 | 위 + 자기 스크랩 | 자기 스크랩만 |
| 수집기 | 전체 | 기사·사건 (비밀 키, 프런트에 절대 안 넣음) |

**브라우저에 들어가는 키로는 절대 쓰기가 되면 안 된다.** 프런트 JS의 키는 누구나 볼 수 있다.

---

## 11. 이관·운영

**매일 06:40 수집 / 08:00 발송이 돌고 있다. 멈추지 않고 옮긴다.**

```
1  DB 만들고 기존 120건 이관          기존 파이프라인 그대로
2  수집기가 DB에도 쓴다 (JSON도 계속)  둘 다 쓴다. 어긋나면 JSON이 정답
3  프런트를 API로 전환                문제 생기면 되돌림
4  메일 스크립트를 API로 전환
5  JSON 생성 중단, 리포 정리          ← 여기서부터 되돌리기 어려움
--- 여기까지는 지금 규모. 아래가 새로 붙는다 ---
6  넓은 층(GDELT) 적재                AI 비용 0. 규모 감각을 여기서 얻는다
7  좁은 범위로 추출 시험              비용을 실측한다. 전면 실행 전에
8  범위를 넓히며 반복                 E 결정: "필요한 만큼"
```

**6~8이 v2에서 새로 생긴 단계다.** 7번을 건너뛰고 8번으로 가면 안 된다 —
실측 없이 전면 추출을 돌리면 되돌릴 수 없는 비용이 나간다.

### 운영

| 항목 | 권고 |
| --- | --- |
| 백업 | **원문 우선.** 원문만 있으면 나머지는 다시 계산된다 |
| 비용 감시 | **추출 비용에 상한을 건다.** 저장 비용은 볼 필요 없다 |
| 고장 감지 | "오늘 기사 0건이면 알림" 한 줄이 가장 값싼 감시 |
| 검수 | 하지 않기로 결정됨(세션 22). `review: "pending"`이 정상 |

---

## 사용자 결정 현황

| # | 질문 | 상태 |
| --- | --- | --- |
| A | 저장소 | **재검토함.** 결론: 관리형 Postgres + 객체 저장소. **단 1순위 결정이 아니다** |
| B | GitHub Pages 유지 | **확정 — 유지.** DB 후 정적 페이지는 껍데기가 맞다 |
| C | 항목 구분이 최적인가 | **아니었다. 이 v2가 개정판** (11항목 4개 층) |
| D | 계정·스크랩 | **확정 — 자리만 잡고 지금 만들지 않는다** |
| E | 소급 범위 | **확정 — 온톨로지를 구축하며 필요한 만큼.** 비용 구조상 옳은 판단 |

## 다음에 정할 것

E를 "필요한 만큼"으로 정했으므로, **1번(획득)과 2번(추출 예산)이 다음 결정**이다.

- 넓은 층을 GDELT로 갈 것인가 — 무료이고 `ROADMAP.md`가 이미 조사해뒀다
- 첫 시험 범위를 어디로 잡을 것인가 — 한 국가 · 한 달 정도면 실측이 나온다
- API 종량 과금으로 넘어가는 것을 받아들일 것인가 — 대규모 소급의 전제 조건이다
